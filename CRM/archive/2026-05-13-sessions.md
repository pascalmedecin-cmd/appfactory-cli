# Archive sessions condensées

## Sommaire

- S180 - Cascade audit 360 vague V3b (28 Low + 12 Info) → CASCADE ENTIÈREMENT FERMÉE (livré 2026-05-12)
- S181 - API Google Places comme 7e source de prospection (livré 2026-05-12)
- Recap status block « Cascade audit 360 — LIVRÉE (S178→S180) » (sommaire 7 vagues)
- Recap status block « Refonte page Aide CRM + polish — LIVRÉE (S182-S183) » (sommaire reconstruction Aide)
- [WATCH] cockpit entries CRM `blocked`/`block_role` stale (post-V3a-1) — RÉSOLU 2026-05-11

---

## S180 - Cascade audit 360 vague V3b (28 Low + 12 Info) → CASCADE ENTIÈREMENT FERMÉE

Fait 2026-05-12 (xhigh, ~5h). 3 commits ff main : `b9e5dc6` (L-01..L-16 sécu+bugs+contracts) + `f9cc188` (L-17..L-28+I-01..I-12 code+UI+infra) + `<docs>` (doctrine Tailwind, CSP/RLS assumées, GOLDEN §14.4-14.5, clôture). Tag `pre-v3b-2026-05-12`. Highlights : HSTS posé ; CSP `unsafe-inline` documentée ; DELETE photos/visits ownership-aware logging + 404 ; `src/lib/utils/time-constants.ts` (≈12 consumers migrés) ; token `--ease-out-expo` (≈28 usages) ; `.github/dependabot.yml` ; `@vitest/coverage-v8` + `npm run coverage` (≈58,5% .ts) ; mémoire `feedback_rls_multitenant_durcissement_si_4_users.md`. 1 migration prod (`20260512_001` cost_audit_runs numeric 10,6→12,6, appliquée+vérifiée). 8 SKIP hors-scope nommé. +17 Vitest (1094/1094), svelte-check 0 err, build OK, smoke prod 8 routes 303→/login + cron 401. Audits Opus : security 0C/0H/0M/0L/3I (artefact `audit_secu_2026-05-12_audit-360-vague-3b.md`) ; test-coverage 13/15 → 2 gaps comblés. Entry cockpit `14efa600`. Cascade 360 fermée : 136 findings, 134 fixés, 2 (H-28/M-29) résorbés par la refonte Aide S182. Détail complet : `archive/2026-05-12-sessions.md`.

## S181 - API Google Places comme 7e source de prospection

Fait 2026-05-12 (high, ~3,5h). Endpoint `src/routes/api/prospection/google-places/{+server,helpers}.ts` (Places API New `searchText`, 7 métiers, bounding boxes 6 cantons romands, dédup intra + cross-source vs Zefix) + `quota/+server.ts` + `src/lib/server/quota.ts` (table `api_quota_log`, réservation atomique du slot avant l'appel = ferme TOCTOU, cap 900/mois sous le seuil gratuit ~1000). 2 migrations prod appliquées+vérifiées (`20260512_002` api_quota_log + RPC ; `20260512_003` CHECK source). Intégration `config.ts`/`api-limits.ts`/`schemas.ts`/`prospection-utils.ts` + UI onglet « Google Places » dans `ImportModal.svelte` (token `--color-prosp-place`) + garde anti-source-payante dans `saveRecherche` (→400). +59 Vitest (1153/1153), svelte-check 0 err, build OK. Audits Opus : security 0C/0H/1M résolu/1L résolu/6I (artefact `audit_secu_2026-05-12_google-places.md`) ; test-coverage 12/15 → 3 gaps comblés. Entry cockpit `f0cc23e7` livrée. Clé Vercel prod posée. Spec `notes/google-places-2026-05-12/spec.md`. [WATCH] : clause `ESCAPE` à la RPC `entreprises_lookup_by_name`. Détail complet : `archive/2026-05-12-sessions.md`.

## Recap status block - Cascade audit 360 — LIVRÉE (S178→S180, 2026-05-10..12)

7 vagues, 134/136 findings fixés (7C V1 + 32H V2a/V2b/V2c + 57M V3a-1/V3a-2 + 28L+12I V3b), 2 délégués (H-28 /aide hors charte, M-29 getElementById /aide). 13 migrations SQL prod. Audits Opus cumul 0C/0H/0M. Vitest 1094/1094. Détails par vague : `archive/2026-05-10-sessions.md`. Tags rollback : `pre-audit-360-cascade-2026-05-10` / `pre-v3b-2026-05-12` / `audit-360-cascade-close-2026-05-12`.

## Recap status block - Refonte page Aide CRM + polish — LIVRÉE (S182 from-scratch, S183 polish diagrammes/respiration/nav mobile)

`/aide` reconstruite S182 (3 niveaux data-driven, recherche full-text, 5 diagrammes SVG, zéro `getElementById`, GOLDEN v9 → résorbe H-28 + M-29). Polish S183 : diagrammes lisibles (clip « +3 pts » corrigé, textes débordants raccourcis, flèches nettoyées, schéma « écosystème » refondu en vue d'ensemble simple, chiffres recentrés dans les bulles d'étape), tirets longs retirés, respiration accrue sur les blocs texte ; nav mobile = le menu latéral se ferme à la sélection d'une page. Voir Livré S182/S183 dans ce fichier. Hors-scope nommé reporté en tâches dormantes : tour guidé interactif, bouton « ? » contextuel sur les 9 pages, push première-connexion vers /aide.

## S182 - Refonte page Aide CRM from scratch (3 niveaux, data-driven, GOLDEN v9)

Fait 2026-05-12 (xhigh, ~2,5h). `/aide` reconstruite : couche logique `src/lib/aide/{content,search,checklist}.ts` + composants `AideBlock`/`AideDiagram`/`AideChecklist` + orchestrateur `(app)/aide/+page.svelte` (1443→553 lignes). 3 niveaux (Prise en main / Fonctions détaillées / Doc technique), 5 diagrammes SVG, recherche full-text insensible casse/accents, deep-link `?tab=&section=`, sommaire sticky, zéro `getElementById`/`{@html}`, GOLDEN v9. Résorbe H-28 + M-29. +39 Vitest (1192/1192), svelte-check 0 err, build OK. Audits Opus : security 0C/0H/0M ; test-coverage 11/13 → 2 gaps comblés. Entry cockpit `eed7761c` livrée. Spec `notes/refonte-aide-2026-05-12/spec.md`. Commit `26fd3e8` poussé. Hors-scope reporté : tour guidé, bouton « ? » contextuel, push première-connexion. Détail complet : `archive/2026-05-12-sessions.md`.

## S183 - Polish page Aide (diagrammes + respiration + tirets longs) + fermeture menu mobile + e-mail de lancement Antoine

Fait 2026-05-12 (high, ~3h). 5 diagrammes SVG `/aide` audités/corrigés (clip « +3 pts » → barres 88→80px, textes débordants raccourcis, flèches nettoyées), schéma « écosystème » refondu en vue d'ensemble pédagogique (tableau de bord en bandeau + 6 écrans en 2 familles, zéro flèche), chiffres recentrés dans les bulles d'étape (`dominant-baseline: central`) ; tirets longs retirés de tous les fichiers Aide ; respiration accrue (line-height 1.6→1.7, paddings sections 24/32→32/40, etc., échelle 8px) ; nav mobile : menu latéral se ferme à la sélection (`onNavigate` sur `Sidebar.svelte` + fix `$effect` cassé dans `(app)/+layout.svelte` — `prevPath` repassé en `let` non réactif) ; e-mail HTML de lancement `notes/email-lancement-antoine-2026-05-12.html` créé (tables + CSS inline + 600px, logo FilmPro inline SVG blanc, 3 étapes connexion, 4 fonctions clés, mini-schéma, non commité). Gates : 1192/1192 Vitest, svelte-check 0 err, build OK. Commits `0f820a2`+`e1eed24` poussés. Reste : smoke prod `/aide` + nav mobile + arbitrage Pascal sur l'e-mail. Détail complet : `archive/2026-05-12-sessions.md`.

## S184 - Cleanup hygiène cockpit : 2 entries livrées (branches mortes caduque + image > 1 Mo)

Fait 2026-05-13 (high, ~0,3h). T1 `0d00274c` caduque : `git branch --merged main` ne retourne que `* main`. T2 `3815a446` 2 PNG `docs/golden/v{5,6}/mobile/signaux.png` 7.2 Mo : v5 supprimé (archive S125 sans référence), v6 conservé (régénéré activement par `tests/mobile.spec.ts`). Commit `20fa290`.

## S185 - Cadrage page Log CRM (feedback bugs/suggestions livraison Antoine) + spec figée 15 critères

Fait 2026-05-13 (xhigh, ~1h). /dig 360 + lecture exhaustive code existant (Sidebar, hooks auth, schemas Zod, migrations RLS récentes, content.ts Aide, ConfirmModal). Spec rédigée `CRM/notes/page-log-2026-05-13/spec.md` (~440 lignes, 15 critères binaires, schéma DB, format export JSON, statuts, helpers admin, tests, gates QA). 4 questions Pascal tranchées : Q1 bouton flottant OK + raccourci clavier RETIRÉ ; Q2 « Log » (vs « Retours ») ; Q3 lecture seule post-envoi ; Q4 export JSON tableau d'objets. Arbitrage mobile : FAB + page /log **desktop-only** via media query `@media (max-width: 1023px)` ; entry cockpit `d068a79d` créée pour futur audit mobile CRM.

## S185 - Page Log CRM LIVRÉE (15 critères binaires, 58 nouveaux Vitest, 0 C/H/M audits Opus)

Fait 2026-05-13 (xhigh, ~3h). Exec spec § 12 en 5 lots commit. **Lot 1 (DB + lib pure + schemas)** : `supabase/migrations/20260513_001_feedback_entries.sql` (table + 3 RLS + 2 CHECK + 3 index + trigger + comparaison `lower(auth.jwt()->>'email')`) ; 6 modules `src/lib/feedback/*.ts` (types, options, admin `isAdminEmail`, pages prefix-match, export `toExportJson` Blob, error-capture singleton cap 3 + purge 60 s) ; 3 schemas Zod (`FeedbackCreateSchema` `.refine` bug↔severity, `FeedbackUpdateStatusSchema`, `FeedbackUpdateNotesSchema`). **Lot 2 (composants)** : `FeedbackButton.svelte` (FAB masqué `<1024px`+`/log`+`/login*`), `FeedbackForm.svelte`, `FeedbackTable.svelte`, `Log` ajouté `config.ts navigation.secondary` + flag `desktopOnly:true`, `Sidebar.svelte` media query. **Lot 3 (page + actions)** : `+page.svelte` + `+page.server.ts` (3 form actions `create`/`updateStatus`/`updateAdminNotes` ; defense in depth `isAdminEmail` AVANT mutation). **Lot 4 (tests)** : `feedback.test.ts` 45 tests + `log-actions.test.ts` 13 tests. 1250/1250 Vitest. **Lot 5 (audits)** : security 0C/0H/0M + 1 Low fixé in-session (RLS UPDATE `lower(email)`) + 4 Info ; test-coverage 7/7 ; contracts enums alignés 4 sources + 1 M documentaire + 4 L. Migration prod appliquée via skill `supabase` + `pg` lib. Spec : `notes/page-log-2026-05-13/spec.md`. Entry cockpit `af586294`.

## S186 - Cleanup post-livraison page Log + smoke prod admin Chrome MCP (7 fix audit S185 + 3 polish Low contracts + smoke 10/10)

Fait 2026-05-13 (high, ~2h). **7 fix audit S185 (a-g)** : (a) `hooks.server.ts` ajout `/log/* POST` rate limit (Info-1), (b) spec ligne 70 retrait `NOT NULL` sur `created_by` (M-1), (c) `+page.server.ts` `user.email.trim().toLowerCase()` (L-2), (d) `load` normalisation `context` via `FeedbackContextSchema.parse` + cast enums (L-4), (e) regen complète `src/lib/database.types.ts` via `supabase gen types` (les 7× `as never` retirés, cascade dette V2a/V2b/V3a M-19 résolue), (f) `error-capture.browser.test.ts` 3 tests, (g) migration `_002` CHECK durci `admin_notes IS NULL OR char_length BETWEEN 1 AND 2000`. Migration `_002` appliquée prod via `pg` lib + `dotenv` chargement `.env.local`. **3 polish Low contracts** : drift spec § 4 RLS lower, `FeedbackUpdateNotesSchema` Zod transform au boundary, `updateAdminNotes` simplifié 1 ligne. **Gates** : 1253/1253 Vitest, svelte-check 0 err (baseline 7 → 0 post regen), build OK. **Audits Opus** : security 0 C/H/M/L + 3 Info ; contracts 20/20 + 4 Low (3 fixés + 1 [WATCH] brand type). **Smoke prod admin Chrome MCP** : 10/10 critères verts (FAB→modale→submit→toast→/log→expand→statut→export JSON UTF-8 OK). 3 résidus côté Pascal restants (Antoine OTP + mobile DevTools + nav burger mobile) → entry cockpit `9e0ff2ac`, 5-10 min. Commit `62015ea` pushed (8 fichiers, +575/-17).

## S186bis - Fix cron Zefix (bascule `/company/search` saturé HTTP 400 → `/sogc/bydate/{date}` + parsing créations Romandie 7j + fix mojibake encoding)

Fait 2026-05-13 (high, ~1,5h). Vérif BDD prod : `signaux_affaires` 131 SIMAP uniquement, **0 entrée Zefix jamais importée**. Test API Zefix `/company/search` : HTTP 400 `RESULTLIST_TO_LARGE` sur les 6 cantons GE/VD/VS/NE/FR/JU. **Bascule** sur `GET /api/v1/sogc/bydate/{date}` : 1 appel/jour × 7j, filtre code `registryOfCommerceCanton ∈ Romandie` + `mutationTypes` contient `status.neu`. Test local : **350 créations Romandie/7j détectables** (jours 10-11 mai = weekend = 0, autres 53-82). 91% des créations ont objet social extrait via parsing `message` SOGC (regex `\b(But|Zweck|Scopo|Purpose)\s*:\s*`). **Fix encoding** : helper `fixZefixMojibake(s) = Buffer.from(s, 'latin1').toString('utf8')` inverse double encoding latin-1→utf-8 (`sociÃ©tÃ©` → `société`). Entités HTML décodées. **Dédup global** par `source_id = uid`. **404 SOGC** (weekends) traité non-erreur. Test V3b L-11 adapté. Suite cron 6/6 verts. Commit `8a534f2` (2 fichiers, +189/-106). Prochain run réel 6h UTC demain matin, impact attendu 40-80 nouvelles entrées source `zefix`.

## Watch list S178 (post-V2c) — archivée S187

- **[WATCH] V3a rate limit /api/entreprises/*** : audit Opus security V2b L-1 — endpoint search non couvert par rate limit. Surface faible (auth gate, mono-tenant ≤10 admins, LIMIT 20 + GIN trigram). Reco : ajouter `/api/entreprises/` dans `hooks.server.ts`.
- **[WATCH] V3a regen `supabase gen types`** — RÉSOLU S186 (cast `as never` retirés post regen complète).
- **[WATCH] V3a TOCTOU Zefix H-02** : Info I-3 audit. Window TOCTOU sub-2s entre SELECT notes_libres et UPDATE. Fix : `UPDATE … SET notes_libres = COALESCE(NULLIF(notes_libres, ''), $purpose)` atomique.
- **[WATCH] V3a `runWithConcurrency` AbortSignal** : Info I-4. Side-effects post-reject possibles si futur consumer fait write dans `fn`. Ajouter `AbortSignal` côté worker.
- **[WATCH] V3a pattern cancelled flag** : étendre H-05 VisitsPanel cleanup à PhotosPanel + autres panels async (race conditions UI silencieuses).
- **[WATCH] V3a audit consumers `entreprises.raison_sociale`** : aligner toute recherche client-side sur `lower(immutable_unaccent(...))` ou forcer RPC. N1 LIKE injection a montré defense-in-depth client doit être à la source.
- **[WATCH] V3a CHECK `jsonb_array_length(items) <= 15`** : defense-in-depth pour F1 cap saturated. Migration possible.
- **[WATCH] V3a audit cross-codebase UPDATE intelligence_reports** : trigger 009 force le bump auto. Vérifier qu'aucun caller ne dépend d'un comportement "no-op UPDATE" stable (run-generation, cron archive, recheck-historical).
- **[WATCH] V3a feedback_rpc_atomique_idempotence.md à graver** : pattern checklist obligatoire pour toute nouvelle RPC plpgsql (idempotence + EXCEPTION handlers + ERRCODE distincts).
- **[WATCH] V3a `Tabs.svelte` glue DOM non testée** : primitive a tests sur logique pure `tabsNav.ts` (14 tests), pas test DOM composant. Repo sans jsdom configuré. Trigger d'ajout = 3e régression a11y.
- **[WATCH] V3a `aria-controls` Tabs vers panels inexistants** : `Tabs.svelte:65` pointe vers IDs `panel-{key}` absents sur pages workspace. Reco : rendre `aria-controls` optionnel sur la primitive.

## Watch list S178 (post-V1) — archivée S187

- **[WATCH] V2b H-06 trigram entreprises** : V1 a posé `.limit(1000)` garde-fou, refonte trigram + ilike bounded prévue V2b H-06.
- **[WATCH] V3a M-19 Zod refit searchch insert** — RÉSOLU S186 (cast `as never` retirés post regen).
- **[WATCH] F3 V2 mobile saisie lead express** : `lead_express` accepté par DB CHECK (migration `20260510_002`), prochain test terrain à confirmer.
- **[WATCH] svelte-check baseline 0 erreur** : V1 a ramené baseline de 4 → 0. Si modifs font remonter erreurs, viser maintien baseline 0.
- **[WATCH] Consolidation cockpit S178 calculée mais apply refusé local strict** : audit déterministe a flaggé 2 tâches `effort.score=3` mais 4 critères True. Ré-applicable via UI après ajustement.
- **[WATCH] Durcissement RLS si 4e user non-fondateur** : tant que FilmPro = 3 fondateurs symétriques, RLS « tous voient tout » assumée. Le jour où 4e user non-fondateur → policies `created_by = auth.uid()` + tests d'intégration RLS. → voir `memory/feedback_rls_multitenant_durcissement_si_4_users.md`.

## Watch list S171 (post-livraison) — archivée S187

- **[WATCH] Compteur quota search.ch côté DB** : pas d'API publique pour lire compteur quota mensuel. Pour visibilité ops → table `api_quota_log` ou colonne incrémentale. Pas critique tant que Pascal contrôle l'usage, à graver tâche si usage atteint 50%+ du quota.
- **[WATCH] `prospect_leads.description` consommée par LLM future** : description = `occupation + categories.join(' / ')` (categories user-influenced via `was=` search.ch). Aucun pipeline LLM ne consomme aujourd'hui. Si pipeline future → cross-check verbatim pattern S168. → voir `memory/audit_secu_2026-05-06_searchch_endpoint.md`.
- **[WATCH] `entry.sourceUrl` rendu href client** : nouvelle colonne `prospect_leads.source_url` peut pointer vers URL search.ch arbitraire. UI rendant `<a href={sourceUrl}>` doit confirmer filtrage `https?://` côté client.
- **[WATCH] Race condition `addItem` veille** : form action sur `/veille/[id]` fait read-modify-write non-atomique sur JSONB via service client. Si 2 admins simultanés, le second écrase. Mitigation : optimistic locking colonne `version` si volume augmente. → voir `memory/audit_secu_2026-05-05_veille_themes_db.md` Low #3.
- **[WATCH] Injection prompt LLM via description thème** : description admin concaténée dans `SYSTEM_PROMPT` via `buildThemesPromptSection`. Zod limite 500 chars mais aucun escape. Cross-check verbatim S168 protège la sortie. Mitigation : escape `\n` + délimiter `<themes_taxonomy>` XML-like.

---

## [WATCH] cockpit entries CRM `blocked`/`block_role` stale (post-V3a-1) — RÉSOLU 2026-05-11

Réconciliation manuelle de `entries.jsonl` + `blocks-crm.json` (backups `*.bak.<ts>-pre-reconcile-v3a`) : `78249fed4e8d` (V3a-2) → `blocked: false, block_role: actionable, block_num: 1` (Bloc #1) ; `14efa600e6ba` (V3b) → `block_role: bloque, block_num: B1` ; `dc3f7cf2133a` (Aide) → `blocked: true, block_role: bloque, block_num: B2`. `blocks-crm.json` retitré (Bloc #1 = « Cascade audit 360 V3a-2 », B1 = V3b, B2 = Aide). `/start` + `/api/consolidate/blocks` alignés sur la « Prochaine attaque » CLAUDE.md. Cause racine inchangée (`cli_pivot.py` ne réécrit pas `blocked`/`block_role`/`status` sur entries `source=cli` existantes) → si une future consolidation LLM s'applique, re-vérifier ; sinon stable.
