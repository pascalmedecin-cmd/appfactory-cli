# Archive sessions condensées

## Sommaire
- S178 - Cascade audit 360 V3a-1 (29 Medium sécu défense-en-profondeur + bugs latents + contracts + code, 2 migrations SQL prod, 4 audits Opus 0C/0H/0M, 2026-05-11)
- S178 - Cascade audit 360 V2c (13 High UI golden v9 + tests gate global + CSS dedup + 3 nouveaux tokens, 2026-05-11)
- S178 - Cascade audit 360 V2b (7 High concurrence/atomicité + 6 zero-debt bug-hunter F1..F6 + N1 LIKE escape + 6 migrations SQL prod, 2026-05-10)
- S178 - Cascade audit 360 V2a (9 High sécu/contracts/DB CHECK + L-01 zero-debt + 2 migrations SQL prod, 2026-05-10)
- S178 - Cascade audit 360 V1 (7 Critiques + H-11 cause racine + 14 drifts collatéraux + 2 migrations SQL prod, 2026-05-10)

---

- [x] ~~S178 - Cascade audit 360 V3a-1 livrée : 29 Medium sécu défense-en-profondeur + bugs latents + contracts + code~~ - Fait 2026-05-11 (S178 suite, xhigh, ~6h cumulé). Branche `fix/audit-360-vague-3a-1` mergée ff main, 4 commits : `a9db0e3` sécu+bugs (M-01..M-15 hors M-08) + `7291069` contracts+code (M-16..M-27 hors M-20) + `2453e3d` migrations SQL M-08+M-20 (appliquées prod) + `d3b6513` retours 4 audits Opus (zéro dette). **Items fixés (27 ; M-28/M-29 SKIP volontaire)** : (a) **Sécu** M-01 `sanitizeError` sur les exceptions des endpoints zefix/simap/enrichir-batch (defense-in-depth fuite clé via `String(err)`, cas `fetch failed` Node 20+) ; M-02 nouveau helper `src/lib/utils/safe-url.ts` (`safeHttpUrl` whitelist http/https via `new URL()`, rejette `javascript:`/`data:`/`vbscript:`/`file:`/relatif/protocol-relative) appliqué sur `lead.source_url` dans LeadSlideOut.svelte ; M-03 `auth/callback/+server.ts` : `error.message` sanitize + tronqué 200 chars avant l'URL `?detail=...` (historique navigateur + logs referrer Vercel) et console.error ; M-04 rate limit étendu à POST /login (anti cost-burn SMTP via `?/sendcode` bombing ; GET /login reste libre) ; M-10 `sanitizeForLog` : redact AVANT slice (un secret chevauchant `maxLen` ne fuite plus son préfixe tronqué) + nouveau pattern query param `?key=...` (clé search.ch). (b) **Bugs latents** M-05 DI explicite du `CostTracker` : `runWeeklyGeneration` crée son propre tracker, passé à `generateIntelligenceReport`/`crossCheckBatch`/`persistRunCosts` (5 sites) — plus de singleton module-level partagé entre invocations concurrentes ; M-06 dashboard `/+page.server.ts` relances : exclut les deals clos (gagné/perdu) ET inclut explicitement `etape_pipeline IS NULL` via `.or(etape_pipeline.is.null,etape_pipeline.not.in.(gagne,perdu))` (nouvelle constante `ETAPES_PIPELINE_CLOSED` dans schemas.ts ; le `.not(...,in,...)` initial excluait les NULL en logique ternaire SQL → bug-hunter Medium fixé) ; M-07 `(app)/+layout.svelte` : cleanup `clearTimeout` dans le `$effect` (annule l'ancien si la route change avant 150 ms) ; M-09 `apply-signals.ts` : upsert + recompute lead par lead (au lieu de bulk upsert puis batch recompute) — réduit la fenêtre où un lead a un signal lié mais un score pas encore recalculé (transaction atomique impossible : calcul de score = JS, pas SQL) ; M-11 `hooks.server.ts` : timer de nettoyage rate-limit arrêté en HMR dev via `import.meta.hot.dispose` (tree-shaké en prod) ; M-12 `recomputeLeadScoresBatch` : `runWithConcurrency` concurrence bornée (paramètre optionnel, défaut 4, garde `Math.max(1,...)`) au lieu d'une boucle séquentielle O(N)×3 round-trips ; M-13 cron `nettoyage-crm/+server.ts` : `BATCH_LIMIT` 200→100 + `export const config = { maxDuration: 300 }` (anti-timeout Vercel : 200×~0,7s ≈ 140s) ; M-14 rate limiter extrait dans `src/lib/server/rate-limiter.ts` (`createRateLimiter` avec éviction LRU `map.keys().next().value` au lieu de fail-closed à 10 000 entrées — un attaquant rotant ses IP ne verrouille plus les clients légitimes ; `dispose()` + `setInterval().unref()`) ; M-15 `verifyUrlsBatch` : `runWithConcurrency` concurrence bornée 4 au lieu de `Promise.all` illimité (cousin H-03). (c) **Contracts** M-16 `pipeline/+page.server.ts` : `PipelineOpportuniteRowSchema` Zod `.passthrough()` (dans `pipelineFormat.ts`) — `load` filtre les lignes dont `id` (PK NOT NULL) est absent/mal typé + logge id + paths Zod ; `montant_estime` tolère number OU string (numeric DB peut transiter en string ; aval tolère déjà) ; M-17 `coerceFormBoolean(value)` dans schemas.ts (`'true'`/`'on'`/`'1'` insensible casse+trim → true) — câblé sur prospection (`alerte_active`) + veille/themes (`active` ×2), remplace 3 `=== 'true'` ad-hoc ; M-18 `.max()` en amont des regex date (`intelligence/schema.ts` `published_at` `.max(40)` partie `(\.\d+)?` variable-longueur, `week_label` `.max(12)` ; `schemas.ts` `date_relance_prevue`/`date_publication` `.max(10)` ×4 ; `ManualItemSchema.published_at` `.max(10)`) ; M-19 `readReportItems(raw, reportId)` dans `src/lib/server/intelligence/report-items.ts` — `IntelligenceItemSchema.array().safeParse()` du JSONB `intelligence_reports.items` au boundary de lecture, fallback brut + `console.warn` si édition legacy ; câblé sur `/veille` (liste) et `/veille/[id]` (détail) ; M-20 cohérence type `prospect_visits.entreprise_id` : fix littéral `ALTER COLUMN TYPE uuid` **infaisable** (FK vers `entreprises.id text` legacy ; migrer `entreprises.id` = hors scope) → migration `20260511_002` : `CHECK (entreprise_id IS NULL OR entreprise_id ~* '^[0-9a-f]{8}-...')` (insensible casse, mirror du `UUID_RE /i` app) sur `prospect_photos.entreprise_id` ET `prospect_visits.entreprise_id` (audit data 2026-05-11 : 0 ligne hors format) + commentaires de colonne ; M-21 export CSV : header HTTP `X-Export-Schema-Version: N` (`EXPORT_SCHEMA_VERSION` dans csv-export.ts) — **pas** de ligne `#` dans le corps CSV (un parseur naïf la traiterait comme en-tête ; flaggé par 2 audits) ; M-22 `isAllowedThemeSlug(slug, allowedSlugs)` dans `src/lib/server/intelligence/theme-slug.ts` — validation slug thème partagée entre `generate.ts` (fallback gracieux post-Zod) et `/veille/[id]` (refus item manuel thème inconnu/inactif), remplace 2 patterns dupliqués. (d) **Code** M-23 16 `font-size: X.5px` (PipelineCard/Column/DropPlaceholder + dashboard sub-composants) → entiers de l'échelle GOLDEN (`13.5→14`, `12.5→12`, `11.5→12`) ; M-24 `src/lib/utils/format-percent.ts` source unique : `formatPercentFromRatio(ratio, {signed?, fallback?})` (style:'percent') + `formatPercentValue(value, {fallback?})` (0..100, décimal + ` %` ASCII) avec garde NaN/Infinity → fallback `—` ; `coutsFormat.formatPercent` et `reportingFormat.formatPercent` délèguent désormais ; M-25 commentaire dans `signauxFormat.ts` documentant l'asymétrie casing des clés de tab (snake_case = aligné enum DB `statut_traitement`, kebab-case ailleurs = regroupements UI-only) ; M-26 `(data as any)` cast `contacts/+page.svelte` : déjà retiré par le refactor V2b H-06 (autocomplete async) — vérifié, plus aucun `as any` dans pages/composants ; M-27 `ScorePill.svelte` : hex codés en dur (chaud/tiède bg/text/icon) → `color-mix(in srgb, var(--color-danger/-warning) X%, white|black)` (atténué fond / saturé texte+icône, cf. H-24 TriageQueue V2c) ; froid icône → `var(--color-info)` ; froid texte conservé en hex `#3F4D5F` (valeur tunée WCAG AA, documentée). **Migrations SQL prod** : `20260511_001` (fonction `enforce_prospect_photos_cap()` `SET search_path = public, pg_catalog`, pas `SECURITY DEFINER` + trigger `BEFORE INSERT` sur `prospect_photos` + `pg_advisory_xact_lock(hashtext('prospect_photos_cap:'||owner))` rend le COUNT atomique + `RAISE EXCEPTION ... USING ERRCODE = 'P0010'` → endpoint `/api/photos` matche sur l'ERRCODE custom → 409 + cleanup storage) + `20260511_002` (2 CHECK format UUID `~*`). Appliquées prod via Session pooler eu-west-1 (DATABASE_URL_ADMIN gitignored `.env.local`), re-appliquées après corrections audits, vérifiées (function SET search_path ✓ pas SECURITY DEFINER ✓ P0010 ✓ ; CHECK `~*` ✓ ; trigger présent ✓). Audit pré-migration : `prospect_photos` 0 trigger, `entreprises` 1 ligne 0 non-uuid, `prospect_visits`+`prospect_photos` 0 ligne. **Tests** : Vitest **1013/1013 verts** (baseline V2c 968 + ~45 : safe-url 8, rate-limiter 6, hooks 2 (M-04 POST/GET /login), apply-signals 1, recompute-score 1, format-percent 5, theme-slug 4, report-items 4, schemas 5 (coerceFormBoolean, ETAPES_PIPELINE_CLOSED, cap date), csv-export 1, pipelineFormat 5 (PipelineOpportuniteRowSchema), sanitize 2 (régression redact-avant-slice JWT chevauchant maxLen + `?key=`) + ajustements). svelte-check **0 erreur** (35 warnings préexistants inchangés). Build prod OK (`@sveltejs/adapter-vercel`). **4 audits Opus parallèles** sur le diff branche (~57 fichiers, +1008/-203) : (1) security-auditor 0 C / 0 H / 0 M / 0 L / 5 Info — OWASP **11/11 vert** ; 2 Info corrigés zéro dette (search_path, ERRCODE P0010), 1 corrigé (CSV `#` line retirée), 2 acceptés watchlist (collision `hashtext` advisory lock = perf only ; rate-limiter dégradé sous rotation >10k IP = modèle mono-tenant) ; (2) contracts-reviewer 0 nouveau drift Zod↔DB, **21/21 contract checks** ; 3 Low corrigés zéro dette (`montant_estime` number|string, CHECK `~*`, CSV `#` line), 3 Info acceptés ; (3) bug-hunter **0 race condition résiduelle** (rate-limiter/`runWithConcurrency`/DI CostTracker/apply-signals par lead/trigger advisory lock tous corrects, pas de deadlock) + **1 Medium fonctionnel** = régression introduite par M-06 (`.not(etape_pipeline,in,...)` sur colonne nullable excluait les NULL → dashboard relances se vidait en prod, toutes les opps actuellement à NULL) → **fixé** ; 3 patterns Low acceptés (warn pipeline sans id → fixé, `verifyUrlsBatch` export mort → V3b, dépendance ERRCODE P0010 surfacé par PostgREST → confiance moyenne, test intégration confirmerait) ; (4) test-coverage-reviewer ~85-90% chemins critiques touchés, **0 lacune bloquante** ; 1 « Important » comblé (test régression `sanitizeForLog` redact-avant-slice + `?key=`), reste renvoyé V3a-2 (test endpoint /api/photos mapping P0010→409 = M-54), `coerceFormBoolean(File)` edge Low. Tous findings actionnables corrigés zéro dette (commit `d3b6513`). **Smoke prod** 8 routes (`/`, `/reporting`, `/dashboard/couts`, `/signaux`, `/pipeline`, `/contacts`, `/entreprises`, `/veille`) HTTP 303 → /login ; `/api/export/leads` 303 ; `/api/cron/signaux` 401 (CRON_SECRET intact). Artefact `~/.claude/projects/-Users-pascal-Claude-Projets-AppFactory/memory/audit_secu_2026-05-11_audit-360-vague-3a-1.md`. **Cockpit** : entry V3a `78249fed4e8d` NON delivered (V3a-1 = moitié de V3a ; à livrer après V3a-2). **Reste cascade** : V3a-2 (28 items UI golden v9 polish M-30..M-47 + tests M-49/M-51..M-57, ~8h, spec `notes/audit-360-2026-05-09/spec-vague-3a.md` § UI + § Tests) + V3b (28 Low + 12 Info ~6h) = ~14h en sessions autonomes suivantes. **PAUSE après V3a-1** (décision Pascal : on décide ensuite si on enchaîne V3a-2). **Watchlist post-livraison** : (a) ERRCODE P0010 → 409 endpoint photos (confiance moyenne que PostgREST surface bien les SQLSTATE classe P0 custom ; test intégration V3a-2 M-54 confirmerait) ; (b) `verifyUrlsBatch` export mort (aucun caller ; `generate.ts` boucle sur `verifyUrl` un par un) — cleanup V3b ; (c) rate-limiter sous rotation >10k IP : éviction LRU dégrade le rate-limiting sans bloquer les légitimes — OK mono-tenant, si surface s'élargit → store partagé ; (d) `runWeeklyGeneration` concurrence=4 réel de `recomputeLeadScoresBatch` non exercé directement (tests forcent `=1` pour le déterminisme du mock FIFO ; couvert via `concurrency.test.ts`) ; (e) note `pg` installé éphémère `node_modules` (gitignored, `--no-save --no-package-lock`) pour appliquer les migrations — nettoyé au prochain `npm ci`.


---

- [x] ~~S178 - Cascade audit 360 V2c livrée : 13 High UI golden v9 + tests gate global + CSS dedup~~ - Fait 2026-05-11 (S178 suite, xhigh, ~5h cumulé). 4 commits push origin/main appfactory-cli mergés ff main (branche `fix/audit-360-vague-2c`, locale supprimée) : `0d0ddd6` (H-16..H-20 tests gate global + extraction `image-validate.ts`, 7 fichiers +864/-47) + `ecc61f4` (H-21/H-23/H-24/H-25/H-26/H-27/H-29/H-30/H-31 UI golden v9 + CSS dedup + tokens, 47 fichiers +641/-1040) + `1f62ea6` (doc GOLDEN_STANDARD § radius.2xl + ombres carte + échelle éditoriale dashboard, +31/-4) + `b685641` (durcissement tests post test-coverage-reviewer, +34). **Items fixés (13 High)** : (a) **Tests gate global** : H-16 `src/routes/login/login-actions.test.ts` (10 tests sendcode/verifycode — whitelist domaine, OTP, cookie 7j httpOnly) ; H-17 `src/routes/auth/callback/callback-handler.test.ts` (5 tests verifyOtp + exchangeCodeForSession succeed/fail/no-op) ; H-18 `src/hooks.server.test.ts` (14 tests gate global — rate limit 429 sur 3 préfixes, session expirée delete cookie + signOut, email hors whitelist 403, `user.email === undefined` fail-closed, routes exempt /login + /api/cron/* + /api/intelligence/recheck-historical, headers CSP/XFO/nosniff/referrer/permissions, charset utf-8 JSON) ; H-19 extraction `src/lib/server/photos/image-validate.ts` (`detectImageType` + `validatePhotoUpload` pures, `api/photos/+server.ts` refactoré pour les consommer) + `image-validate.test.ts` (16 tests magic bytes PNG/JPEG/WEBP/HEIC, PDF spoof rejeté, MIME mismatch, > 5 Mo 413, MAX_PHOTO_BYTES = 5 Mo) ; H-20 `addItem-validation.test.ts` (8 tests pipeline anti-hallu — URL valide ajouté, URL morte 404, paywall, denylist, contenu court, thème inactif, published_at mal formé, unauthorized 401). Code prod auth (`hooks.server.ts`, `login/+page.server.ts`, `auth/callback/+server.ts`, `veille/[id]/+page.server.ts`) **inchangé** — seuls les tests ajoutés. (b) **CSS dette** : H-21 `src/lib/styles/workspace.css` (`ws-page`/`ws-page-actions`/`ws-content`/`ws-btn*`/`ws-filter-select`/`ws-fab` + media queries + prefers-reduced-motion), importée dans `(app)/+layout.svelte`, 4 pages workspace (signaux/pipeline/contacts/entreprises) migrées vers les classes `ws-*`, ~340L de CSS dupliqué supprimées ; H-23 token sweep — ~60 `border-radius` hardcodés → `var(--radius-sm/md/lg/xl/2xl/full)` (perl sur 30 fichiers, hors `/prospection` figée + `/aide` refonte H-28), ~30 `box-shadow` → `var(--shadow-card/-hover/-active)` (3 nouveaux tokens app.css pour le pattern carte éditoriale récurrent : KpisBento/RelancesList/ActiviteTimeline/AlertesStrip/PipelineCard/Column/TriageQueue/SignauxCards/EntreprisesCards) + `color-mix(in srgb, var(--color-*) X%, transparent)` pour les glows primary/success/text ; résiduels documentés : `CoutsChart` 2px (largeur de barre, pas de token), `ProspectionTabs` figée (`12px 12px 0 0` ×2, `999px`, `8px`), cercles `50%` (formes, pas des résiduels). (c) **UI golden v9** : H-24 6 hex hardcodés `TriageQueue` (`#6E9C8F #C28A86 #8A95A8 #7B8FAE #027A48 #C0391A`) → `color-mix(var(--color-success/danger/info/primary))` (repos atténué, hover saturé) ; H-25 gradients dashboard retirés — `SectionGreeting` prénom gradient text → aplat `var(--color-primary)`, `KpisBento` radial 280×280 → aplat translucide `rgba(255,255,255,0.045)`, `TriageQueue` footer linear → `var(--color-surface-alt)` (aligne sur GOLDEN § 6 anti-pattern gradient) ; H-26 H1 unique par page — `Header.svelte` garde le `<h1>`, les titres de contenu (`reporting`, `dashboard/couts`, `veille`, `veille/[id]`, `veille/themes`, `SectionGreeting` dashboard) passent en `<h2>` (hiérarchie h1>h2>h3, `page-has-heading-one` OK partout via Header qui rend toujours `<h1>{nav label}</h1>`) ; H-27 primitive `src/lib/components/Tabs.svelte` (roving tabindex `active=0 / inactifs=-1` + navigation clavier ArrowLeft/Right/Up/Down/Home/End avec activation auto) + `src/lib/components/tabsNav.ts` (logique pure) + `tabsNav.test.ts` (14 tests), les 5 composants `*Tabs.svelte` (Contacts/Entreprises/Signaux/Pipeline/Reporting) deviennent de simples wrappers ; H-29 login charte — `<svg>` inline (Heroicons mail) → `<Icon name="mail" size={20}>` Lucide, police `'Inter'` retirée de `.login-subtitle` (DM Sans héritée), `gap`/`padding` `.login-card` `2.5rem` (= Tailwind 10, retiré GOLDEN § 4) → `2rem`, overlay `rgba(10,22,40,0.45)` → `color-mix(var(--color-primary-dark) 45%)`, fix conflit `text-sm text-lg` sur input code → `text-lg` ; H-30 type scale dashboard — échelle éditoriale 24/40/56/76 documentée GOLDEN § 3.2bis (réservée à `/`), `SectionGreeting.hero-h1` 44px → 40px, `KpisBento.kpi-split` mobile 36px → 32px ; H-31 `KpisBento.kpi-card` radius 24px → `var(--radius-2xl)` (16px, nouveau token app.css + GOLDEN § 2.6, cohérent avec `RelancesList`/`ActiviteTimeline`/`TriageQueue` toutes les cartes éditoriales dashboard). **Tests** : Vitest **968/968 verts** (+66 vs baseline V2b 902 = 70 tests gate global : login 10 + callback 5 + hooks 14 + image-validate 16 + addItem-validation 8 + tabsNav 14 - 3 ajustements). svelte-check **0 erreur** (baseline V2b maintenue, 35 warnings préexistants inchangés). Build prod **14.6s** OK. **Audit Opus security-auditor** sur diff ~54 fichiers : 0 C / 0 H / 0 M / 0 L / 2 Info acceptés (a11y `aria-controls` Tabs vers panels inexistants — pré-existant les 5 composants Tabs ; validation MIME/taille redondante endpoint photos — défense en profondeur intentionnelle). OWASP **11/11 vert**. Surface réelle d'attaque : aucun nouveau `{@html}`, chaîne anti MIME-spoofing photos préservée byte-for-byte (signatures magic bytes identiques, ordre validation identique, cleanup orphelin storage préservé), `tabsNav.nextTabIndex` bornes vérifiées, `color-mix` CSS pur, aucune modif auth/RLS/réseau/headers (code prod auth inchangé), CSRF SvelteKit native intact, aucune modif `package.json`. **Audit Opus test-coverage-reviewer** : couverture suffisante, 0 lacune bloquante, ~85-90% chemins critiques gate global ; suivis durcis in-session (cas `user.email === undefined` fail-closed flaggé Medium, rate limit `/api/prospection/*`+`/api/visits*`, `published_at` mal formé) ; watch non bloquant : `Tabs.svelte` glue DOM non testée (repo sans jsdom) — voir watch list. Artefact `~/.claude/projects/-Users-pascal-Claude-Projets-AppFactory/memory/audit_secu_2026-05-11_audit-360-vague-2c.md`. **GOLDEN_STANDARD.md** mis à jour : § 2.6 `radius.2xl`, § 3.2bis échelle éditoriale dashboard, § 3.3 tokens ombre carte, § 6 note retrait gradients, § 9.2 coquille workspace partagée + titre page h2. **Smoke prod** 8 routes (`/`, `/reporting`, `/dashboard/couts`, `/signaux`, `/pipeline`, `/contacts`, `/entreprises`, `/veille`) HTTP 303 → /login + redirect chain `/signaux` → 303 /login → 200 confirmée + nouvelle build déployée vérifiée (`--radius-2xl` + `--shadow-card` présents dans `/_app/immutable/assets/0.D4spvN49.css`). **Cockpit deliver** entry `7b8be5e018c6` `via: http, ok: true, block_marked_delivered: true`. **Tag rollback** `pre-v3a-2026-05-11` posé + pushé origin. **Critères acceptation** 21/24 ✓ — AC-10 (axe-core via Playwright), AC-17 (Playwright e2e), AC-21 (ui-auditor visual) non exécutés en session autonome car nécessitent `tests/auth-setup.ts` (login OTP interactif manuel pour générer `tests/.auth.json`) ; la structure h1/h2 (Header rend toujours `<h1>`, contenu en `<h2>`) et la primitive Tabs ont été code-reviewées par les 2 subagents Opus, et les gates svelte-check/Vitest/build sont vertes. **Reste cascade** : V3a (57 Medium ~15h, spec `notes/audit-360-2026-05-09/spec-vague-3a.md`, découpage facultatif V3a-1/V3a-2) + V3b (28 Low + 12 Info ~6h, spec `spec-vague-3b.md`) = ~21h en sessions autonomes suivantes. **Watchlist post-livraison** : (a) `Tabs.svelte` glue DOM non testée (jsdom absent du repo) — trigger d'ajout = 3e régression a11y workspace tabs ; (b) `aria-controls` Tabs vers panels inexistants sur les pages workspace (pré-existant, centralisé V2c) — rendre optionnel sur la primitive en V3a ; (c) `/api/entreprises/search` toujours non couvert par le rate limit (`hooks.server.ts:38-50`) — flaggé V2b L-1, fix 1 ligne en V3a (Medium defense-in-depth) ; (d) le `workspace.css` `ws-*` comme pattern à propager si d'autres pages workspace émergent.


---

- [x] ~~S178 - Cascade audit 360 V2b livrée : 7 High concurrence+atomicité + 6 zero-debt audit Opus bug-hunter (F1..F6) + 1 zero-debt N1 LIKE escape + 6 migrations SQL prod~~ - Fait 2026-05-10 (xhigh, ~5h cumulé). 3 commits push origin/main appfactory-cli sur branche `fix/audit-360-vague-2b` mergée ff main + cleanup branche local + remote : `fd4cbb7` (V2b initial 18 fichiers : 7 modifiés + 11 nouveaux dont 3 migrations + 5 tests, 1061 insertions / 169 deletions) + `f50af95` (zero-debt audit Opus bug-hunter F1..F6 11 fichiers : 8 modifiés + 3 nouvelles migrations, 377 insertions / 66 deletions) + `fed5081` (zero-debt audit Opus bug-hunter round 2 N1 LIKE escape, 1 fichier 13 insertions / 1 deletion). **Items fixés** : (a) **Concurrence/atomicité spec V2b 7 High** : H-02 enrichir Zefix preserve notes_libres user-saisis (SELECT existing avant update + `if (purpose && !existing.notes_libres)`) ; H-03 recheck-historical Promise.all illimité → `runWithConcurrency(items, 4, fn)` via nouveau helper `src/lib/server/utils/concurrency.ts` (worker pool ordered, +6 tests TDD) ; H-05 VisitsPanel.svelte flag `cancelled` + onDestroy + checks après chaque await (loadVisits/checkIn/confirmDelete) ; H-06 endpoint `/api/entreprises/search?q=` ilike trigram-backed bounded LIMIT 20 (escape `% _ \\`, len max 100, +6 tests TDD) + getOrCreateEntreprise contacts/+page.server.ts via RPC normalisée + migration `20260510_005` pg_trgm + GIN index + autocomplete frontend +page.svelte debounced 250ms async (drop response stale via seq counter) + retrait pré-fetch full list + extension join contacts.entreprises avec site_web pour logo Clearbit ; H-08 unread `(app)/+layout.server.ts` `.is('archived_at', null)` aligne sidebar avec /veille cards visibles ; H-09 veille/[id]/+page.server.ts addItem optimistic locking SELECT items+version puis UPDATE WHERE id+version=$old + retry max 3× sur conflit + migration `20260510_006` ADD COLUMN version INTEGER NOT NULL DEFAULT 0 ; H-10 prospection/+page.server.ts transferer RPC `transfer_lead_to_crm(uuid)` plpgsql + migration `20260510_007` (transaction implicite Postgres rollback auto si throw, retour JSON entreprise_id+contact_id). (b) **Zero-debt audit Opus bug-hunter round 1 (3H+3M)** : F1 cap rank=15 saturated → `if (currentItems.length >= 15) return fail(409, …)` avant calcul newRank (l'optimistic lock empêche lost updates de version mais pas les ranks dupliqués si items.length déjà 15) ; F2 RPC transfer_lead non-idempotent → migration `20260510_008` ajoute guard `IF v_lead.statut='transfere' RAISE EXCEPTION P0001 lead_already_transferred` + `EXCEPTION WHEN unique_violation` fallback SELECT entreprise homonyme via UNIQUE INDEX V1 + côté SvelteKit map P0001 → fail(409) "Lead déjà transféré" ; F3 trigger bump_version → migration `20260510_009` BEFORE UPDATE force `NEW.version = OLD.version + 1` sauf si `NEW.version > OLD.version` (CAS optimistic addItem respecté). Tous writers (cron archive, recheck-historical, run-generation) bumpent désormais auto, addItem détecte tout changement intercurrent ; F4 unread reads filter → 2 queries : SELECT ids actifs WHERE archived_at IS NULL puis COUNT reads `.in('report_id', activeIds)`, élimine drift permanent post-archive (avant : reads d'éditions archivées comptés vs total réduit = badge erroné permanent) ; F5 lookup entreprise normalisé → migration `20260510_010` RPC `entreprises_lookup_by_name(p_query text) RETURNS TABLE(id text, raison_sociale text)` SQL STABLE qui matche `lower(immutable_unaccent(raison_sociale)) LIKE lower(immutable_unaccent(p_query)) || '%' LIMIT 50`. Aligne avec UNIQUE INDEX partial V1 (sans cela "École Suisse SA" en DB + saisie "ecole" rate match côté ILIKE brut → INSERT 23505 silencieux) ; F6 VisitsPanel cleanup pendingDelete inconditionnel finally (avant : cancelled=true skip cleanup → ref Visit retenue jusqu'au GC). (c) **Zero-debt round 2 (1M)** : N1 LIKE wildcard injection RPC lookup → helper `escapeLikePattern` côté SvelteKit (escape `\` `%` `_`) avant rpc(). Sans escape, saisie "Foo%" matchait toutes entreprises commençant par "foo" (filtre JS normalize rattrapait majorité mais defense-in-depth manquante à la source). **Workflow branche** : `git tag pre-v2b-2026-05-10` posé S178 V2a → `git checkout -b fix/audit-360-vague-2b` → 3 commits → `git push -u origin fix/...` → preview Vercel implicite → `git checkout main && git merge --ff-only` + `git push origin main` → prod Ready ~70s + smoke prod 5/5 routes 303→/login (`/contacts`, `/entreprises`, `/veille`, `/prospection`, `/api/entreprises/search?q=test`) + `git tag pre-v2c-2026-05-10` + `git branch -d` local + `git push origin :fix/...` remote = clean. **Migrations prod appliquées** via Session pooler eu-west-1 IPv4 port 5432 (DATABASE_URL_ADMIN gitignored `.env.local`) : audit pré-migration 005 `pg_trgm_installed=0` (sera installé), audit pré-migration 006 `version_col_exists=0` (clean ADD COLUMN), audit pré-migration 007/008 `rpc_exists=0` (clean CREATE), audit pré-migration 009 `trigger_version=0` (clean CREATE), audit pré-migration 010 `rpc_lookup=0` (clean CREATE). Verify post-migration : 6/6 (pg_trgm=1, trgm_index=1, version_col=1, rpc_transfer=1, trigger_version=1, rpc_lookup=1). Bonus : entreprises.id colonne type=text (pas uuid) découvert lors apply 010 → migration adaptée RETURNS TABLE(id text, raison_sociale text). **Tests** : Vitest **902/902 verts** (+24 vs baseline V2a 878 : 6 concurrency + 6 entreprises search + 5 layout-unread (3+2 F4) + 4 transferer-rpc (3+1 F2 P0001) + 3 addItem-optimistic (2+1 F1 saturated) = 24). svelte-check **0 erreur** (baseline V2a maintenue), 35 warnings préexistants inchangés. Build prod **12.62s** OK. Casts `as never` (RPC lookup + transfer_lead) + `'version' as 'id'` + `as unknown as string` minimaux pour gérer types Database non-régénérés (tracé V3a regen post-cascade complète). **Audit Opus security-auditor** sur diff 18 fichiers + 6 migrations : 0 C / 0 H / 0 M / 1 L / 4 Info. L-1 watchlist : endpoint `/api/entreprises/search` non couvert par rate limit `hooks.server.ts:38-50` (auth gate OK, mono-tenant ≤10 admins, LIMIT 20 + GIN trigram, defense-in-depth incohérente, fix V2c/V3a 1 ligne). 4 Info : I-1 tests transferer-rpc adversarial à étendre staging V3a, I-2 casts post-regen Supabase types groupé V3a M-19, I-3 TOCTOU H-02 sub-2s entre SELECT existing.notes_libres et UPDATE (fix V3a `UPDATE … SET notes_libres = COALESCE(NULLIF(...), $purpose)` côté DB), I-4 runWithConcurrency AbortSignal V3a si futur consumer write dans fn. OWASP **11/11 verts**. Surface réelle d'attaque : aucun `{@html}`, regex linéaires non-ReDoS (escapeIlike, normalizeCompanyName), RLS Supabase mono-tenant intacte (RPC SECURITY DEFINER non utilisé), CSRF SvelteKit native intact, pas de SQL injection (param `||` plpgsql opère sur valeurs SELECT-bound paramétrées), gen_random_uuid cryptographiquement sûr. Artefact `~/.claude/projects/-Users-pascal-Claude-Projets-AppFactory/memory/audit_secu_2026-05-10_audit-360-vague-2b.md`. **Audit Opus bug-hunter** spécialisé concurrence/atomicité (round 1 : 3H+3M trouvés F1..F6 → fix in-session zero-debt + round 2 sur diff cumulé : 6/6 closed + N1 nouvelle Medium → fix in-session zero-debt). 0 race condition résiduelle confirmée round 2. **Smoke prod** 5/5 routes 303→/login : `https://filmpro-crm.vercel.app/{contacts,entreprises,veille,prospection,api/entreprises/search?q=test}`. **Cockpit deliver** entry `37ffe39f8c16` `via: http, ok: true, block_marked_delivered: true`. **Reste cascade** : V2c (13 H UI golden v9 + tests gate global + CSS dedup ~6h), V3a (57 M ~15h), V3b (28 L + 12 I ~6h). Total ~27h restant sur 3 jours.


---

- [x] ~~S178 - Cascade audit 360 V2a livrée : 9 High sécu+contracts+DB CHECK + 2 migrations SQL prod + L-01 fix in-session zero-debt~~ - Fait 2026-05-10 (xhigh, ~5h cumulé). 1 commit push origin/main appfactory-cli sur branche `fix/audit-360-vague-2a` mergée ff main + cleanup branche local + remote : `10df838` (24 fichiers : 18 modifiés + 6 nouveaux, 3471 insertions / 2452 deletions). **Items fixés** : (a) **Sécu** H-01 npm audit 5 CVEs → 0 vuln (`@anthropic-ai/sdk 0.88→0.95.1` + override `cookie >=0.7.0` ferme CVE-2024-47764 + `@sveltejs/kit 2.55→2.59.1` + postcss + vite bumps non-breaking) ; H-04 `cross-check.ts:332-340` rejet strict `verbatim_ok=false` quelle que soit divergences (brèche zéro hallu fermée, +2 tests TDD) ; H-32 `search-ch:60-71` 403 (clé invalide → 503 + message admin distinct) vs 429 (quota → message ops) aligne pattern S171. (b) **Contracts** H-07 `schemas.ts` regex `^\d{4}-\d{2}-\d{2}$` strict 3 schemas (OpportuniteCreate.date_relance_prevue, SignalCreate.date_publication, LeadCreate.date_publication) +7 tests TDD ; H-12 `coutsFormat.ts:21` `CostRunRowSchema` Zod + `dashboard/couts/+page.server.ts:50` safeParse + fallback empty + log warning + signatures aggregateByWeek/computeKpis/filterRunsByFeature élargies CostRun → CostRunRow (sub-type structural compatible) +6 tests TDD ; H-13 migration `20260510_003_lead_signals_rank_15.sql` ALTER CHECK item_rank 1-10→1-15 sur prospect_lead_signals (audit pré-migration : 14 lignes max=1 → élargissement strict) ; H-14 `src/lib/types/actions.ts` (nouveau) `ActionResult<T>` discriminated union + `dbFail` aligné `success: false as const` + 4 form actions cibles refactored (signaux.deleteBatch + signaux.createOpportunite + prospection.saveRecherche + prospection.createExpress) ; cas spécial `{ambiguous, candidates}` documenté hors ActionResult par design ; +8 tests TDD shape ; H-15 migration `20260510_004_db_check_constraints_signaux_opportunites.sql` 3 CHECK alignés Zod (statut_traitement 5 valeurs / type_signal 7 / etape_pipeline 6, NULL toléré). Audit pré-migration documenté (statut_traitement=nouveau, type_signal=appel_offres, etape_pipeline ∅). (c) **Code** H-22 `src/lib/utils/text-normalize.ts` (nouveau) source unique `normalizeNFD` + `normalizeNFDTrim`. 5 sites migrés (text-utils alias rétro-compat, scoring import, segment-mapper import, ImportModal.svelte import, searchch/helpers alias) +10 tests TDD (accents latins, lowercase, Unicode hors range, idempotence, multi-accents, trim externe). (d) **L-01 fix in-session zero-debt** post-audit Opus : `sanitizeApiKeyInLogs` câblé sur 2 console.error de search-ch/+server.ts (defense-in-depth si search.ch echo de la query string dans le body d'erreur ou TypeError fetch failed). **Workflow branche** : `git checkout -b fix/audit-360-vague-2a` → 1 commit → `git push -u origin fix/...` → `git checkout main && git merge --ff-only` + `git push origin main` → prod Ready + smoke prod 5/5 routes 303→/login (`/api/prospection/search-ch`, `/dashboard/couts`, `/signaux`, `/pipeline`, `/reporting`) + `git tag pre-v2b-2026-05-10` + `git branch -d` local + `git push origin :fix/...` remote = clean. **Migrations prod appliquées** via Session pooler eu-west-1 IPv4 port 5432 (DATABASE_URL_ADMIN gitignored `.env.local`) : audit pré-migration H-13 `SELECT MIN/MAX/COUNT FROM prospect_lead_signals` = 14 lignes min=max=1 (élargissement strict OK), audit pré-migration H-15 `SELECT DISTINCT` sur 3 colonnes = aucune valeur hors enum (statut_traitement={nouveau}, type_signal={appel_offres}, etape_pipeline=∅). **Tests** : Vitest **878/878 verts** (+33 vs baseline V1 845 : 2 H-04 + 7 H-07 + 6 H-12 + 8 H-14 + 10 H-22). svelte-check **0 erreur** (baseline V1 maintenue), 35 warnings préexistants inchangés. Build prod **15.07s** OK. Side-effect : pin `@supabase/supabase-js 2.101.1` + `@supabase/ssr 0.10.0` (caret transitive ramenait 2.105 qui durcit RejectExcessProperties → 6 erreurs TS hors scope V2a). `@types/node` devDep ajouté (perdu au reinstall). **Audit Opus security-auditor** sur diff 24 fichiers + 2 migrations : 0 C / 0 H / 0 M / 0 L post fix L-01 zero-debt / 4 Info acceptés watchlist (cross-check verdict null path conservation par défaut anti-DoS cron, migration NULL toléré aligné optional, CostRunRowSchema NaN propagation à tracer V3a M, alias normalizeText doublon pédagogique V3b). **OWASP 11/11 verts**. Surface réelle d'attaque : aucun `{@html}`, regex linéaires non-ReDoS (text-normalize, coutsFormat, segment-mapper, searchch/helpers), RLS Supabase mono-tenant intacte (migrations strictement non-régressives ADD CONSTRAINT CHECK avec NULL toléré), CSRF SvelteKit native intact, npm audit 0 vuln. Artefact `~/.claude/projects/-Users-pascal-Claude-Projets-AppFactory/memory/audit_secu_2026-05-10_audit-360-vague-2a.md`. **Audit Opus contracts-reviewer** parallèle : 6 contracts DB↔Zod alignés (STATUTS_TRAITEMENT, TYPES_SIGNAL, ETAPES_PIPELINE, IntelligenceItem.rank 1-15, cost_audit_runs feature/status), ActionResult contract conforme, 0 nouveau drift. 2 watchlist V2b/V3a : (a) Medium canton 'Autre' DB CHECK vs Zod CANTONS_LEAD pas inclus (ligne historique potentielle ferait échouer LeadCreateSchema), (b) Low propagation discriminated union 7 actions encore en ancien shape. **Smoke prod** 5/5 routes 303→/login (`/api/prospection/search-ch`, `/dashboard/couts`, `/signaux`, `/pipeline`, `/reporting`). **Cockpit deliver** entry `0b8ebc3a` `via: http, ok: true`. **Reste cascade** : V2b (7 H concurrence/atomicité ~4h), V2c (13 H UI golden v9 + tests + CSS dedup ~6h), V3a (57 M ~15h), V3b (28 L + 12 I ~6h). Total ~31h restant sur 3 jours. **Watchlist post-livraison** : (a) Surveiller éventuelle ligne `prospect_leads.canton='Autre'` historique (drift V2b/V3a M), (b) propagation discriminated union 7 form actions restantes (V2b/V3a L), (c) CostRunRowSchema NaN propagation à durcir avec `.refine((n) => Number.isFinite(n))` V3a M, (d) F3 V2 mobile saisie lead express continue de fonctionner avec CHECK lead_express + nouveaux CHECK enum (test terrain à confirmer prochaine sortie commerciale).


---

- [x] ~~S178 - Cascade audit 360 V1 livrée : 7 Critiques + H-11 cause racine (tâche #3 livraison client lundi 2026-05-11)~~ - Fait 2026-05-10 (xhigh, ~3.5h cumulé). 2 commits push origin/main appfactory-cli sur branche `fix/audit-360-vague-1` mergée ff main + cleanup branche local + remote : `3573f1a` (fix V1 7 Critiques + H-11 + 14 drifts collatéraux + 2 migrations SQL) + `4442cb9` (zero-debt audit Opus M getOrCreateEntreprise cap 1000). **Triage Pascal** 136/136 approved (`notes/audit-360-2026-05-09/decisions-2026-05-10.md`) → 6 specs séquentielles autonomes V1→V3b figées (`notes/audit-360-2026-05-09/spec-vague-{1,2a,2b,2c,3a,3b}.md` + `README-vagues.md`). **Critiques fixés** : C-01 /reporting alias PostgREST `date_creation:date_ajout`/`date_import_ajout` ; C-02 import `STATUTS_LEAD.transfere` enum (vs string `'transfere_crm'`) ; C-03 /api/export/[entity] 5 occurrences renommées (`created_at→date_import`, `score→score_pertinence`, `date_creation→date_ajout`/`date_import_ajout` selon entité) + literal union `ExportTable` ; C-04 `lib/server/csv-export.ts` mitigation OWASP CSV formula injection (préfix apostrophe si 1er char ∈ `[=+\-@\t\r]`) + 10 tests TDD ; C-05 helper `getOrCreateEntreprise` (refactor 2 sites contacts/+page.server.ts + handle 23505 unique violation retry) + migration `20260510_001_entreprises_raison_sociale_unique.sql` (extension unaccent + wrapper IMMUTABLE `immutable_unaccent` + UNIQUE INDEX partial sur `lower(immutable_unaccent(raison_sociale)) WHERE statut_archive=false`) ; C-06 `DataTable.svelte` cleanup `onDestroy` (clearTimeout searchTimer + activeResizeCleanup pour pointer drag listeners interruption) ; C-07 migration `20260510_002_prospect_leads_source_lead_express.sql` ALTER CHECK source autorise `'lead_express'` (débloque F3 V2 mobile saisie rapide). **H-11 cause racine** : `App.Locals.supabase: SupabaseClient<Database>` dans `src/app.d.ts` → svelte-check révèle 14 drifts cachés tous fixés in-session zéro dette (apply-signals.test ChipCanton type narrow, run-generation cast `Json` x4 + cast `as never` searchch insert avec commentaire pointant V3a M-19, pipeline `montant_estime` narrow `typeof === 'number'`, prospection sortKey literal union `(typeof VALID_SORT_KEYS)[number]`, regbl/simap/zefix `if (e.source_id)` guards null, +page.svelte/pipeline/+page.svelte/prospection/+page.svelte literal types alignés via signatures minimales `{ id: string }` + lookup interne ou champs subset explicites). **Workflow branche** : `git tag pre-audit-360-cascade-2026-05-10` (rollback) → `git checkout -b fix/audit-360-vague-1` → 2 commits → `git push -u origin fix/...` → preview Vercel implicite → `git checkout main && git merge --ff-only` + `git push origin main` → prod Ready ~70s + smoke prod 5/5 routes 303→/login (`/`, `/reporting`, `/contacts`, `/signaux`, `/api/export/leads`) + `git tag pre-v2a-2026-05-10` + `git branch -d` local + `git push origin :fix/...` remote = clean. **Migrations prod appliquées** via Session pooler eu-west-1 IPv4 port 5432 (DATABASE_URL_ADMIN gitignored `.env.local`) : audit doublons pré-migration `SELECT lower(immutable_unaccent(raison_sociale)), count(*) ... HAVING count > 1` = vide (0 doublon, ~100 entreprises actuelles), audit valeurs source pré-ALTER CHECK = `simap 81 / regbl 50 / zefix 9 / search_ch 5` (aucun drift). **Tests** : Vitest **846/846 verts** (+10 vs baseline /signaux 836 = 10 tests TDD csv-export formula injection : `=cmd|"/c calc"!A1` + `+41791234567` + `-100` + `@user` + `\tcol` + `\rdata` + `a=b` non préfixé + `user@domain.com` non préfixé + chaîne vide + nombre positif). svelte-check **0 erreur** (vs 4 baseline pré-H-11, vs 14 post-H-11 avant fixes drifts) ; 35 warnings préexistants Svelte 5 inchangés (V2c les traitera). Build prod 16.82s OK. **Audit Opus security-auditor** sur diff 18 fichiers + 2 migrations : 0 C / 0 H / 0 M (1 M `getOrCreateEntreprise` SELECT full-table flagged + fixé in-session zéro dette = `.limit(1000)` + commentaire pointant V2b H-06 refonte trigram + ilike bounded) / 1 L `as never` searchch insert tracé V3a M-19 / 4 Info acceptés watchlist (migration unaccent + immutable wrapper conformes pattern Postgres wiki, ALTER CHECK aligné modèle de menace mono-tenant FilmPro, typage `<Database>` zéro impact runtime/permissions/RLS, DataTable cleanup pas de double-free, regex CSV couvre 5 triggers OWASP). **OWASP 11/11 vert**. Surface réelle d'attaque : aucun `{@html}`, ReDoS regex CSV linéaire, RLS Supabase mono-tenant intacte (typage `<Database>` est pur TypeScript), CRON_SECRET conservé, sanitizeForLog appliqué `Json` cast run-generation. Artefact `~/.claude/projects/-Users-pascal-Claude-Projets-AppFactory/memory/audit_secu_2026-05-10_audit-360-vague-1.md`. **Smoke prod** `https://filmpro-crm.vercel.app/{reporting,contacts,signaux,api/export/leads,/}` HTTP 303 → /login HTTP 200 (auth gate fonctionne, redirect chain confirmée curl). **Critères acceptation** V1 spec : 16/16 ✓. **Cockpit deliver** entry `7ed58fdb` `via: http, ok: true`. **Reste cascade** : V2a (9 H sécu/contracts/DB CHECK ~5h), V2b (7 H concurrence/atomicité ~4h), V2c (13 H UI golden v9 + tests gate global + CSS dedup ~6h), V3a (57 M ~15h), V3b (28 L + 12 I ~6h). Total ~36h restant sur 4 jours. **Watchlist post-livraison** : (a) `getOrCreateEntreprise` cap 1000 actuel, refonte trigram V2b H-06 ; (b) `as never` searchch tracé V3a M-19 ; (c) Surveiller F3 V2 mobile saisie lead express prochain test terrain (CHECK lead_express maintenant accepté DB).


## S179-S180 - Cascade audit 360 vagues V3a-2 + V3b (2026-05-12)

- [x] ~~S179 - Cascade audit 360 vague V3a-2 (28 items : 18 UI golden v9 polish M-30..M-47 + 10 tests M-49/M-51..M-57)~~ - Fait 2026-05-12 (xhigh, ~4,5h ; Score 3/4 : multi-étapes + itération coûteuse rebuild + UI non-mesurable). Session autonome. 2 commits mergés ff main : `0c640c6` (UI golden v9 polish) + `9233aa3` (couverture tests). Tag rollback `pre-v3a2-2026-05-12` posé. **UI (M-30..M-47)** : M-30 FAB `.ws-fab` 20px→24px (multiple 8) ; M-32/M-43 `Sidebar.svelte` `<aside>`→`<nav aria-label="Navigation principale">` + `aria-current="page"` sur lien actif + `+layout.svelte` `:global(aside)`→`:global(.sidebar-root)` ; M-33 badge `bg-amber-500`→`bg-warning` (token) ; M-34 slide-out signaux 4×`<h4>`→`<h3>` ; M-35 reporting `.panel-header h2` 14px→18px ; M-36 hero h1 reporting + dashboard/couts 24px→22px ; M-37 `TriageQueue .section-h2` 22px→18px ; M-38 `TriageQueue .ab` 34×34→36×36 ; M-39 `EmptyState` `role="status"`+`aria-live="polite"` ; M-40 signaux `.empty-card-icon.warning` rgba→`color-mix(... var(--color-warning) 10% ...)` ; M-44 signaux `.empty-card h3`→`h2` (+ sélecteur CSS) ; M-46 `Tabs.svelte` prop `countNoun`→badge compteur `aria-label="{count} {noun}"` (passé contacts/entreprises/signaux/pipeline) ; M-41 + M-45 déjà résorbés par V2c (workspace.css `color-mix` + 0 box-shadow rgba) — vérifiés, RAS ; M-31 (boutons pied de modal `h-11` 44px = cible tactile WCAG 2.5.5, ≠ `h-10` toolbar) + M-42 (disque décoratif translucide `.kpi-featured` 280×280 conservé, aplat pas gradient) + M-47 (ligne DataTable cliquable conservée + contrainte "pas de focusable imbriqué hors checkbox" gravée) = **décisions tranchées documentées `GOLDEN_STANDARD.md § 14`**. **Tests** : +64 Vitest (1077/1077) — M-49 form actions `contacts-actions.test.ts`(14, create/update/delete) + `pipeline-actions.test.ts`(7), M-51 `signaux/server.test.ts`(5, garde CRON_SECRET + happy path Zefix+SIMAP vides), M-52 `buildScoreFilter` extrait `cron/alertes/score-filter.ts` + dédup 2 branches via `applyScoreFilter()` + `score-filter.test.ts`(10), M-53 `api/visits/geo-helpers.ts` extrait (haversineMeters, parseOwner/parseOwnerFromBody, parseSwissAddress, geocodeAddress avec queryFn injectable) + `geo-helpers.test.ts`(21), M-54 `export/[entity]/server.test.ts`(6) + **bug `formatJoined` CSV fixé en passant** (repartait de la ligne au lieu de la valeur de cellule → colonne « Entreprise » du CSV contacts toujours vide), M-55 `enrichir-zefix.test.ts`(8, fetch mocké, H-02 préserve notes_libres) ; M-56 `playwright.mobile.config.ts` +2 projets (iphone-se 375×667, pixel-7 412×915) ; M-57 seeders `tests/fixtures.ts` (`ensureSeedLead`/`ensureSeedEntreprise` via form actions, best-effort) → 6/7 `test.skip(true)` de `mobile.spec.ts` déskippés (le 7e « bouton Étape suivante sur fiche entreprise » conditionnel par nature) ; M-48 RISQUE OUVERT « RLS Supabase non couverte par les tests Vitest » gravé `CRM/CLAUDE.md § RISQUES OUVERTS` + mémoire `feedback_rls_mocks_insufficient_S99.md` ; M-50 SKIP (doublon H-09 V2b). **Gates** : svelte-check 0 erreur / 35 warnings (baseline inchangée) ; Vitest 1077/1077 ; build prod OK (19,3s) ; smoke prod 8 routes 303→/login + `/api/cron/signaux` 401. **Audits Opus** : `code-review:security-auditor` 0C/0H/0M/0L/2I (Info : geocodeAddress 3 appels externes ≈7,5s par POST visites = déjà [WATCH] ; buildScoreFilter ne valide pas l'invariant des seuils mais config statique) ; `code-review:test-coverage-reviewer` ~47% des 32 form actions exercées (>30% cible), score 13/15 — gaps Low/Info → watchlist V3b. **Non exécuté en session autonome** : AC-11 (Playwright e2e — `storageState` OTP interactif), AC-17 (ui-auditor — Chrome MCP + app lancée) ; structure h1/h2 + tokens code-reviewés. Artefact `~/.claude/projects/-Users-pascal-Claude-Projets-AppFactory/memory/audit_secu_2026-05-12_audit-360-vague-3a-2.md`. Tag rollback `pre-v3b-2026-05-12` posé. **Reste cascade** : V3b (28 Low + 12 Info, ~6h) puis tâche #4 refonte Aide. **Tracking git** : 30 fichiers (UI + tests + helpers + GOLDEN_STANDARD.md § 14 + CRM/CLAUDE.md § RISQUES OUVERTS). Entry cockpit `78249fed4e8d` delivered (bloc #1 marqué delivered). Cockpit deliver → re-réconcilier `entries.jsonl`/`blocks-crm.json` au prochain `/start` si V3b ne remonte pas en Bloc #1 (cause racine `cli_pivot.py` inchangée). **Watchlist V3b ajoutée** (test-coverage) : `entreprises` {create,update,delete} action tests (de-risque aussi le seeder e2e) ; `applyScoreFilter` switch + `swisstopoQuery` fetch/parse/`<b>`-strip branches non testés ; `parseSwissAddress` token numérique dans le nom de rue ; `tests/fixtures.ts` `actionOk` devrait parser le résultat d'action SvelteKit (pas juste HTTP 2xx) + purge des fixtures `[E2E]` en prod.
- [x] ~~Réconciliation drift cockpit CRM (entries.jsonl + blocks-crm.json) → aligné « Prochaine attaque » V3a-2~~ - Fait 2026-05-11 (low, ~0,3h). Au `/start` : `start_list.py` affichait Bloc #1 = « Refonte page Aide » (actionnable) et V3a/V3b en blocs bloqués B2/B3 — à rebours de la « Prochaine attaque » CLAUDE.md (V3a-2). Cause = `cli_pivot.py` ne réécrit pas `blocked`/`block_role`/`status` sur les entries `source=cli` existantes (cf. [WATCH] post-V3a-1). Fix manuel (backups `entries.jsonl.bak.<ts>-pre-reconcile-v3a` + `blocks-crm.json.bak.<ts>-pre-reconcile-v3a`, flock) : `78249fed` V3a-2 → `blocked:false, block_role:actionable, block_num:1` (Bloc #1) ; `14efa600` V3b → `block_role:bloque, block_num:B1` ; `dc3f7cf2` Aide → `blocked:true, block_role:bloque, block_num:B2`. `blocks-crm.json` : nouveau bloc actionnable #1 « Cascade audit 360 V3a-2 », B1 retitré « Cascade audit 360 V3b », B2 retitré « Refonte page Aide CRM from scratch » (enrichments préservés). Vérifié : `/start` rend l'ordre V3a-2 → V3b → Aide ; `/api/consolidate/blocks/appfactory?subproject=crm` aligné. Pivot `/fin-session` (`cli_pivot_repair.py`) re-run après : `created=7 updated=16 purged_cli=0` (les 7 créés = tâches Formation/Consulting non encore poussées, pas une régression CRM), `block_role`/`block_num`/`blocked` CRM préservés, `effort`/`complexity` rafraîchis. [WATCH] passé RÉSOLU. Bloc auto-géré `<!-- BEGIN CONSOLIDATION -->` du CLAUDE.md encore stale (cite « V3a dépend de V2b ») — se régénérera à la prochaine consolidation LLM, pas touché (zone « ne pas éditer »). `M CLAUDE.md` non commité (sur main, pas de demande de commit) ; `entries.jsonl`/`blocks-crm.json` dans `~/.claude/cockpit/projets/` (gitignored).
- [x] ~~S178 (suite) - CLAUDE.md CRM dégrossi de 108,6k à 41,6k chars (-62 %)~~ - Fait 2026-05-11 (low, ~0,3h). Historique des vagues audit 360 V1/V2a/V2b/V2c (S178) déplacé verbatim dans `archive/2026-05-10-sessions.md` (nouveau, 28,5k, format archives existantes) ; entrées Livré S177 + S176ter ×2 retirées du live (déjà condensées dans `archive/2026-05-09-sessions.md`) ; 4 lignes d'en-tête (Statut / Derniere mise a jour / Session courante / Sessions précédentes) compactées vers état courant + pointeurs vers archives ; pointeur § Livré mis à jour. Zéro régression : déplacement uniquement (rien supprimé, tout dans git history + archives) ; sections `## Prochaine session`, watch lists, Consolidation cockpit, STACK/COUTS/DECISIONS/INFRA/REGLES TECHNIQUES intactes. Reste 258 lignes / 41,6k chars : le seul gros poste résiduel = l'entry Livré V3a-1 (~11k), à archiver à la prochaine clôture pour passer strictement sous le seuil perf 40k. `M CLAUDE.md` + `archive/2026-05-10-sessions.md` non commités (sur main, pas de demande de commit ; à intégrer au prochain batch CRM ou commit dédié si Pascal veut).
