# CRM FilmPro : CLAUDE.md

**Note migration restructure** : ce fichier vit dans `CRM/CLAUDE.md`. Le sous-projet CRM FilmPro est dans `AppFactory/CRM/` (path Vercel `rootDirectory: CRM`). Voie A livrée S173 2026-05-06 (split container CLAUDE.md). Voie B livrée S174 2026-05-07 (rename `template/` → `CRM/` + retrait wizard méta + repointage Vercel). Le `AppFactory/CLAUDE.md` racine est un stub container minimal qui pointe vers les 3 sous-projets (CRM, Consulting, Formation). Voir `~/.claude/projects/-Users-pascal-Claude-Projets-AppFactory/memory/project_appfactory_restructure.md` pour le contexte migration complet.

**Statut :** Phase C, **CASCADE GOLDEN V9 6/6 PAGES COMPLÈTE** (S176ter page 5+6/6 2026-05-09) - /signaux refonte cards uniquement (helpers TDD signauxFormat 254L + 37 tests + 3 composants Indicators/Tabs/Cards + 6 onglets ARIA tablist sticky + 4 KPI flat avec variant warning si nouveaux > 0 et success sur convertis + FAB mobile + ConfirmModal delete et batchDelete + audit Opus 0/0/0/1 Low fixé in-session SignalBatchDeleteSchema validation UUID + cap 500 + 3 Info acceptés) + /veille a11y page liste éditions (cards archives wrappées dans `<a>` cliquables plein + aria-label descriptifs n° édition/week label/items count/date publication/état nouveau + h3 heading hierarchy + focus-visible v9 cohérent + audit Opus 0/0/0/0/2 Info acceptés) = **6/6 cascade golden v9 livrée** (dashboard S175 + pipeline + contacts + entreprises S176bis + signaux + veille S176ter). ENTREPRISES V9 REFONTE LIVRÉ (S176bis page 4/6) - helpers entreprisesFormat + 4 composants Indicators/Tabs/ViewToggle/Cards + toggle 2 vues + grille mère 8px stricte + skill refactoring-ui § Grid foundation enrichi + fix DataTable overflow-hidden. CONTACTS V9 REFONTE LIVRÉ (S176bis page 3/6). PIPELINE V9 REFONTE LIVRÉ (S176bis page 2/6). DASHBOARD V9 REFONTE LIVRÉ (S175 page 1/6). MIGRATION RESTRUCTURE VOIE A+B LIVRÉE (S173-S174). GOLDEN STANDARD v9 LAYERED DS LIVRÉ (S172) + règle `noDashedLines` ajoutée S176bis. search.ch 2e source découverte LIVRÉ (S171). Pipeline veille ANTI-HALLUCINATION V2 LIVRÉ (S168). /prospection FIGÉE PAGE MODÈLE GOLDEN V9 (S165). Cron veille externalisée GHA (S167). Audit UX/UI 360 LIVRÉ (S160). V2 mobile terrain CLOS (S127α+S129β+S130γ). Formation IA = sous-projet autonome dans `Formation/`, `cc` option 5.
**Derniere mise a jour :** 2026-05-09 (S176ter xhigh : 3 livrables intégralement en prod = (1) Cascade golden v9 6/6 COMPLÈTE pages 5+6 /signaux refonte cards uniquement (helpers TDD signauxFormat 254L + 37 tests + 3 composants Indicators/Tabs/Cards + 6 onglets ARIA tablist + 4 KPI flat + FAB mobile + ConfirmModal delete et batchDelete + audit Opus 0/0/0/1 Low fixé in-session SignalBatchDeleteSchema validation UUID + cap 500 + 3 Info acceptés) + /veille a11y page liste éditions (cards `<a>` cliquables plein + aria-label descriptifs + h3 hierarchy + focus-visible v9 + audit Opus 0/0/0/0/2 Info acceptés). (2) Audit zéro hallucination W19 cron auto GHA 1er run autonome avec taxonomie DB livrée S169 = 3/3 items propres, 0 hallu grave, 1 paraphrase mineure ~100 vs 107 baux Carl-Vogt + 1 paraphrase technique façonnage vs seaming acceptables, 0 doublon URL W18/W19. Pipeline V2 anti-hallu S168 tient sur cron auto autonome. (3) Suppression handlers legacy `/api/cron/intelligence` + `/api/intelligence/trigger` + retrait bypass auth `/api/intelligence/trigger` dans hooks.server.ts + docstring cost-tracker.ts updaté cite désormais workflow GHA cron-veille comme consumer canonique = surface d'attaque RÉDUITE post-externalisation S167 (un attaquant ayant leaké CRON_SECRET ne peut plus déclencher timeout 504 cost-burn sur Vercel via ces 2 routes spécifiques). Aucun import orphelin (runWeeklyGeneration + buildVeilleDepsFromEnvObject restent consommés par scripts/run-veille.ts + 25 tests). CRON_SECRET conservé (utilisé par 6 endpoints actifs : signaux, alertes, nettoyage-crm, intelligence-archive, lead-rescore, recheck-historical). Audit Opus 0/0/0/0/1 Info no-debt cosmétique. **5 commits push origin/main appfactory-cli** : `080dc34` /signaux refonte + `ea467b6` /veille a11y + `8648523` doc clôture cascade 6/6 + `c213be8` suppression handlers legacy + `33a5464` doc clôture finale. **Smoke prod** : `https://filmpro-crm.vercel.app/signaux` 303→/login 200, `/veille` 303→200, `/api/cron/intelligence` GET 404 (deleted), `/api/intelligence/trigger` POST 303→/login (auth gate avant 404, surface RÉDUITE), `/api/cron/signaux` + `/api/cron/intelligence-archive` 401 Bearer (endpoints actifs intacts). **Tests** Vitest **777/777 verts** (+37 vs baseline /entreprises 740 = 37 tests signauxFormat). svelte-check 4/35 baseline strict maintenue. Build prod 16.25s OK. **Backlog `[BLOQUÉ - cron auto GHA vendredi 08/05]` complètement vidé**. 4 mémoires gravées : `audit_secu_2026-05-09_signaux_v9.md` + `audit_secu_2026-05-09_veille_a11y.md` + `audit_veille_2026-05-09_W19_post_pipeline_v2.md` + `audit_secu_2026-05-09_legacy_cron_removal.md`. ─── Précédemment S176bis : Refonte UI /entreprises v9 cascade gabarit golden v9 page 4/6 + grille mère 8px stricte + toggle 2 vues + skill refactoring-ui § Grid foundation enrichi cross-projets. 2 commits push origin/main appfactory-cli sur branche `feat/entreprises-v9` mergée ff main + cleanup branche local + remote : `719506c` Phase 1+2+3+4 (helpers `src/lib/utils/entreprisesFormat.ts` 192L + 24 tests Vitest TDD : entreprisesIndicators total/qualifiees/avecContact/sansCanton, filterEntreprisesByTab/countsByTab 4 tabs Toutes/Qualifiées/À qualifier/Sans contact, emptyMessageForTab, readPersistedView/persistView localStorage SSR-safe try/catch + whitelist stricte 'cards'|'table', logoUrlForSite whitelist HTTP/HTTPS rejette javascript:/data:/ftp:, contactCountForEntreprise ; 4 composants extraits dans `src/lib/components/entreprises/` : EntreprisesIndicators 122L 4 cards flat SANS border-right verticales + variant warn ambre si sansCanton > 0, EntreprisesTabs 137L ARIA tablist 4 onglets sticky + snippet `actions` à droite avec séparateur hairline 32px height + padding-left 24px, EntreprisesViewToggle 68L toggle table/grid 32×32 avec persistance crm.entreprises.view, EntreprisesCards 241L grille auto-fill minmax(320px, 1fr) cartes de visite premium logo 56×56 + Badge statut + 3 rows meta adresse/site/contacts + spring physics cubic-bezier(0.16,1,0.3,1) + diffusion shadow + prefers-reduced-motion respecté ; `+page.svelte` refondu 484 → ~570L orchestrateur preserve SlideOut + ModalForm + ConfirmModal + enrichir Zefix mais ajoute state view+activeTab+searchQuery+derived indicators/counts/tabsSpec/filteredByTab+filtre search post-tab + 2 effects persistance localStorage + render conditionnel `{#if view === 'table'}` DataTable / `{:else}` EntreprisesCards + FAB mobile bottom-right 56×56 primary shadow rgba 45% display:grid < 768px + page-actions display:none < 768px + media queries 1024/768) + icon-map +3 mappings (table_rows/grid_view/location_off) + 3 imports Lucide (Table2/LayoutGrid/MapPinOff) ; `24a9ebf` fix DataTable overflow-hidden sur wrapper extérieur (coins arrondis partiellement gommés par bg-surface-alt du thead débordant — fix universel s'applique aussi à /contacts /pipeline /prospection en silencieux). **Workflow branche** : `git checkout -b feat/entreprises-v9` → 2 commits → `git push -u origin feat/...` → preview Ready 59s URL stable `filmpro-kbw7mmvzh-...vercel.app/entreprises` → Pascal validation visuelle preview (mockup itéré 2× pour grille mère + retrait séparateurs verticaux trompeurs) → `git checkout main && git merge --ff-only` + `git push origin main` → prod Ready 55s → `git branch -d` local + `git push origin :feat/...` remote = clean. **Skill enrichi cross-projets** : `~/.claude/skills-library/refactoring-ui/SKILL.md` § 7 Layout & Composition → nouvelle sous-section « Grid foundation - mandatory before any layout » (3 grilles obligatoires : horizontale gutter 32px constant + 12-col implicite, baseline 8px stricte sans drift 6/14/28, type scale 12/13/14/16/20/24/32 sans drift 13.5/15.5) + 4 zones hiérarchisées workspace (Identity primary focal H1 24px > Pulse indicators 24px discrets > Filters tabs+search > Content). `golden-standard/SKILL.md` § 2.0 → pré-requis bloquant pointe vers refactoring-ui. Skill `refactoring-ui` 19K → 22K, table routing régénérée via `skills_sync.py --regenerate-routing`. **Mémoires gravées** : `feedback_dual_view_table_cards.md` (toggle 2 vues pour pages avec entité visuelle forte, persistance localStorage, default table) + `feedback_open_chrome_with_cache_buster.md` (`open -a "Google Chrome"` + `?v=$(date +%s)` obligatoire pour previews HTML locales, anti faux signal de bug). **Tests** : Vitest **740/740 verts** (+24 vs baseline contacts S176bis = 716 : 24 tests entreprisesFormat). svelte-check **5/35** baseline strict maintenue (5 erreurs préexistantes dans intelligence/prospection/signaux non liées). Build prod **10.31s** OK. **Audit Opus security-auditor** sur diff 8 fichiers +1313/-108 lignes : 0 C / 0 H / 0 M / 0 L / 3 Info acceptés watchlist (Clearbit logo GDPR pré-existant volume cards plus élevé /entreprises, site_web rendu href `rel="noopener noreferrer"` ajouté safe mono-tenant FilmPro, aria-label user-controlled rowAriaLabelFor Svelte 5 auto-escape attribut pattern S163 déjà audité). OWASP **11/11 verts**. Surface réelle d'attaque : aucun `{@html}`, logoUrlForSite whitelist protocole + `new URL()` natif + 1 test explicite anti `javascript:`/`data:`/`ftp:`, localStorage SSR-safe try/catch + whitelist stricte `'cards'|'table'`, 3 regex linéaires non-ReDoS, `+page.server.ts` non modifié donc 4 form actions Zod create/update/delete/enrichir préservées + auth gate `locals.supabase` + RLS Supabase mono-tenant FilmPro intacts + CSRF SvelteKit native. Artefact `~/.claude/projects/-Users-pascal-Claude-Projets-AppFactory/memory/audit_secu_2026-05-08_entreprises_v9.md`. **Smoke prod** `https://filmpro-crm.vercel.app/entreprises` HTTP 303 → /login HTTP 200. **Critères acceptation** : 28/28 ✓ (grille 8px stricte appliquée, type scale stricte, 4 zones hiérarchisées, toggle persistance localStorage, audit sécu 0/0/0/0, FAB mobile, preserve CRUD intégral). **Bonus side-effect** : fix DataTable `overflow-hidden` corrige bug coins arrondis gommés présent silencieusement aussi sur /contacts /pipeline /prospection (toutes les pages workspace cascade). **Watchlist post-livraison** : (a) Clearbit logo proxy GDPR à mitiger ~30 min si client questionne, volume potentiellement plus élevé sur /entreprises (50+ logos par page possible) ; (b) `entry.sourceUrl`/`site_web` rendu href cliquable → confirmer filtrage `https?://` Zod si futur multi-tenant ; (c) si feature future ajoute `{@html}` dans layout → reprendre defense in depth ; (d) le toggle 2 vues comme nouvelle source potentielle archétype golden v10 si pattern se généralise sur d'autres pages (Marketing, Enseignement). ─── Précédemment S176bis : Refonte UI /contacts v9 cascade gabarit golden v9 page 3/6. 3 commits push origin/main appfactory-cli sur branche `feat/contacts-v9` mergée ff main + cleanup branche local + remote : `8b6ff40` Phase 1+2 helpers `src/lib/utils/contactsFormat.ts` + 15 tests Vitest TDD (normalizeCompanyName partagé client+server, contactsIndicators total/prescripteurs/nouveaux mois/sans entreprise, filterContactsByTab, contactsCountsByTab) + 2 composants extraits `src/lib/components/contacts/` (ContactsIndicators 4 cards flat avec variant warning si sans-entreprise > 5, ContactsTabs ARIA tablist 4 onglets Tous/Prescripteurs/À qualifier/Sans entreprise sticky) + `+page.svelte` refondu (450 → ~570 lignes orchestrateur, header condensé bouton Ajouter, DataTable `stickyLeftCols={2}` + `rowAriaLabel={rowAriaLabelFor}` + `emptyMessage={emptyMessageFor(activeTab)}` contextualisé par tab, FAB mobile bottom-right + page-actions display:none < 768px, SlideOut + autocomplete entreprise + ModalForm + ConfirmModal preserve) + Phase 5 mobile media queries 1024/768 ; `a57a537` zero-debt audit sécu Info #1 (drift normalizeCompanyName : `+page.server.ts` importait sa propre copie inline, fix `import { normalizeCompanyName } from '$lib/utils/contactsFormat'` source unique réelle, retrait fonction inline -7 lignes). **Workflow branche** : `git checkout -b feat/contacts-v9` → 2 commits → `git push -u origin feat/...` → preview Ready 55s URL stable `filmpro-ltqjk2t94-...vercel.app/contacts` → Pascal validation visuelle preview → `git checkout main && git merge --ff-only` + `git push origin main` → prod Ready ~70s → `git branch -d` local + `git push origin :feat/...` remote = clean. **Tests** : Vitest **716/716 verts** (+15 vs baseline pipeline S176bis = 701 : 15 tests contactsFormat). svelte-check **5/35** baseline maintenue. Build prod OK. **Audit Opus security-auditor** sur diff 5 fichiers +767/-84 lignes : 0 C / 0 H / 0 M / 0 L / 3 Info dont Info #1 fixé zero-debt (drift normalizeCompanyName), Info #2 Clearbit pré-existant, Info #3 aria-label safe Svelte 5 auto-escape. OWASP **11/11 verts**. Surface réelle d'attaque : aucun `{@html}`, ReDoS regex normalizeCompanyName linéaire safe, autocomplete liste pré-fetched RLS-filtrée, `entreprise_nom` hidden input → Zod côté server inchangé, `+page.server.ts` 3 form actions create/update/delete Zod ContactCreate/Update/DeleteSchema préservés. Artefact `~/.claude/projects/-Users-pascal-Claude-Projets-AppFactory/memory/audit_secu_2026-05-08_contacts_v9.md`. **Smoke prod** `https://filmpro-crm.vercel.app/contacts` HTTP 303 → /login HTTP 200. **Watchlist** : (a) Clearbit logo proxy GDPR à mitiger si client questionne ; (b) drift Zod si nouveaux champs UI ajoutés sans étendre ContactCreateSchema. ─── Précédemment S176bis : Refonte UI /pipeline v9 page 2/6, 3 commits feat/pipeline-v9 mergés (`a38b8a6` helpers + `7baf0c5` 6 composants + `ea85625` zero-debt Low #1+#2) : `a38b8a6` Phase 1 helpers `pipelineFormat.ts` + 19 tests Vitest TDD + `7baf0c5` Phase 2-5 (6 composants extraits `src/lib/components/pipeline/` : Indicators / Tabs / Column / Card / EmptyState / DropPlaceholder + `+page.svelte` refondu 465 → ~410 lignes orchestrateur + FAB mobile bottom-right + media queries 1280/1024/768) + `ea85625` Low #1+#2 zero-debt audit sécu (`bind:this` remplace `getElementById` + guard `data.opportunites.some(o => o.id === id)` defense in depth drop externe). Vercel preview Ready 57s + prod Ready 59s, smoke prod `https://filmpro-crm.vercel.app/pipeline` HTTP 303 → /login HTTP 200. Audit Opus security-auditor sur diff 9 fichiers : 0 C/H/M, 2 Low fixés zéro dette + 3 Info acceptés, OWASP 11/11 verts, artefact `~/.claude/projects/-Users-pascal-Claude-Projets-AppFactory/memory/audit_secu_2026-05-08_pipeline_v9.md`. Tests Vitest **701/701** (+19 helpers pipelineFormat). svelte-check **5/35** baseline -1 warning. Build prod 12.97s. AC : 32/34 ✓ (AC-31 axe-core dev runtime non exécuté + AC-33 ≥ 8 tests neufs largement dépassé). Mockup `notes/refonte-pipeline-2026-05-08/mockup.html` validé Pascal mid-session après 2 itérations (titres colonnes 16px + icônes 36×36 augmentés, retrait dashed). Règle `noDashedLines` inscrite dans `~/.claude/goldens/v9-2026-05-06/tokens.json § rules` + `CRM/GOLDEN_STANDARD.md § 6 anti-patterns`.)
**Derniere revue /optimize :** 2026-04-05
**Prochain bug :** #001
**Session courante :** Session 176ter (CRM, 2026-05-09, `/effort xhigh`, Cascade golden v9 pages 5+6 /signaux refonte cards + /veille a11y livrées = 6/6 COMPLÈTE).
**Sessions précédentes (condensé)** - détail S165-S175 : `archive/2026-05-06-sessions.md` + S174-S175 dans Livré ci-dessous. Détail S122-S125 : `archive/2026-04-28-sessions.md`. Détail S78-S79 : `archive/decisions-sessions-78-79.md`. Détail S70-S77 : `archive/decisions-sessions-70-77.md`. Détail S80-S107 : `Formation/CLAUDE.md` (sous-projet autonome).


---

## SOUS-PROJETS

L'arborescence d'AppFactory héberge des sous-projets autonomes (chacun a son propre repo Git, sa propre stack, son propre CLAUDE.md). Pascal navigue par thème depuis ce dossier.

| Dossier | Repo Git | Statut | URL prod | CLAUDE.md |
|---------|----------|--------|----------|-----------|
| `CRM/` (CRM FilmPro) | `pascalmedecin-cmd/appfactory-cli` (=racine actuelle) | Production | <https://filmpro-crm.vercel.app> | (ce fichier) |
| `Formation/` | `pascalmedecin-cmd/onboarding-ia` (séparé, ignoré dans `.gitignore`) | S1→S7 livrés (12/12 modules en prod) | <https://onboarding-ia.vercel.app> | `Formation/CLAUDE.md` |

> Consulting est sibling autonome depuis 2026-05-07 (S175 Bloc 0 PLAN_ATTAQUE) : path `~/Claude/Projets/Consulting/`, repo Git séparé. Voir son `CLAUDE.md` propre dans `~/Claude/Projets/Consulting/CLAUDE.md`. `cc 4` cd vers ce path.

Pour travailler sur un sous-projet : taper `cc` au terminal et choisir `5. Formation IA` (ou `4. Consulting` qui pointe vers le sibling autonome). Claude Code atterrit directement dans le sous-dossier, charge son `CLAUDE.md` propre (plus léger), et les tâches sont scopées. Les tâches du sous-projet sont tracées dans son CLAUDE.md, pas dans celui-ci.

**`/start` à la racine AppFactory = scope CRM FilmPro** (slug=appfactory, subproject=crm). Affiche les tâches `transmitted` du sous-projet CRM uniquement. Formation IA a sa propre entrée au menu terminal `cc` (cd Formation/ → /start scope Formation IA). Source : `~/.claude/cockpit/projets/appfactory/entries.jsonl` filtré par subproject.

**Extensibilité pédago** (Formation IA) : l'ingestion d'une deep research markdown (marketing aujourd'hui, opération/commercial/autres demain) suit un workflow conversationnel Claude Code CLI piloté par **Opus 4.6**. Règles pédago dans `Formation/docs/PEDAGOGIE.md`, protocole d'ingestion dans `Formation/docs/INGESTION.md`.

---

## QUICK START

```bash
# Ce repo contient le code de l'app CRM FilmPro
# Stack : SvelteKit + Supabase + Vercel
# CLAUDE.md vit dans CRM/ ; le path Vercel rootDirectory est CRM
```

---

## ROLE

Product Engineer sur l'app CRM FilmPro (production, client interne FilmPro). Travail au CLI Claude Code, code-first.

---

## STACK

| Couche | Outil | Role |
|--------|-------|------|
| Frontend | SvelteKit + Tailwind | App web, composants testables |
| Backend | Supabase (PostgreSQL) | BDD, auth, API, stockage |
| Hebergement | Vercel | Deploy auto, previews, domaines, CDN |
| Tests | Vitest + Playwright | Tests unitaires + navigation complete |
| Code | GitHub | Versionne |

---

## COUTS

### Fixes mensuels (operateur)
- Claude Code Max : 100-200 EUR/mois (deja en place)
- Vercel Pro : 20 EUR/mois
- GitHub : 0 EUR
- Supabase Free : 0 EUR (dev/staging)
- **Total : 120-220 EUR/mois**

### Par app client
- Supabase Free : 0 EUR (jusqu'a 500 Mo)
- Supabase Pro : 25 EUR/mois (si depassement)
- Vercel : inclus dans Pro
- Domaine : ~1 EUR/mois (~12 EUR/an)
- **Total : 0-26 EUR/app/mois**

---


## DECISIONS STRUCTURELLES

- Repo separe `appfactory-cli` (ancien `appfactory` reste consultable)
- Workflow prioritaire : construire le cycle core avant d'attaquer FilmPro
- FilmPro = premier projet reel du nouveau workflow (dogfooding)
- Figma Pro abandonne (deep research 2026-04-04 : ratio cout/benefice defavorable pour solopreneur code-first)
- Design = approche code-first : composants custom + kits Figma Community gratuits comme inspiration
- Validation client = prototypes Vercel preview (pas de maquettes Figma)
- CSS scoped obligatoire pour le layout structurel (sidebar, header, nav) : Tailwind responsive (md:hidden, md:block) ne fonctionne pas avec Tailwind v4 pour ce cas
- HTML temporaires pour previsualisations client a chaque etape cle
- Ancien projet AppFactory v1 (Apps Script) = archive consultable, pas de migration

### Decisions UX + Prospection (G36)

→ Archive intégrale : `archive/decisions-structurelles-crm.md` (6 écrans principaux, slide-out panels, saisie rapide, 100% sources gratuites, modèle unifié `prospect_leads`, scoring auto 0-13). Specs prospection complètes : `docs/SPECS_PROSPECTION.md`.

---

## INFRA EN PLACE

- **Prod** : https://filmpro-crm.vercel.app (Vercel, GitHub auto-deploy) + Supabase EU (projet `appfactory`, 10+ tables, RLS active, service role key configurée)
- **Auth** : OTP email 6 chiffres @filmpro.ch + session 7 jours httpOnly ; SMTP Resend (domaine verifié, free plan)
- **APIs** : Zefix REST + search.ch + fal.ai Flux 1.1 Pro Ultra (partage clé avec Enseignement) — Pexels/Unsplash supprimés S67
- **Crons** : `/api/cron/{signaux,alertes,nettoyage-crm,intelligence,intelligence-archive}` tous sécurisés `CRON_SECRET` + service role (Cron `media-enrich` supprimé S67)
- **Tests** : Vitest 164 + Playwright 5 e2e. Accessibilité : focus trap + ConfirmModal partout. Sécurité : Zod sur 19 form actions, rate limiting 10/min, headers CSP/XFO/referrer, timing-safe secrets

→ Détail intégral (env vars, BDD exhaustive, liste tests, liste crons, headers sécurité, pagination serveur) : `archive/infra-crm-detail.md`

## DOCUMENTATION

- `docs/SPECS_PROSPECTION.md` : Specs completes module prospection (sources, modele, scoring, UI, dedup)

→ Inventaire composants EN PLACE (11 composants, 6 pages, 4 API, scripts) archive dans archive/inventaire-composants.md : consulter si besoin de lister les composants existants avant d'en creer de nouveaux

### Historique condensé (archives)

- Sessions 1-8 : UX 6 écrans, design premium Untitled UI/SnowUI, wizards 5 étapes → `archive/decisions-sessions-1-8.md`
- Sessions 9-16 : auth OTP+MFA, Vercel root `template`, PWA, refonte prospection → `archive/decisions-sessions-9-16.md`
- Sessions 70-77 (formation-ia shared) : cadrage parcours + S1-S5 ingestion → `archive/decisions-sessions-70-77.md`
- Sessions 78-79 (formation-ia shared) : S6-S7 ingestion critère sortie → `archive/decisions-sessions-78-79.md`
- Sessions 122-125 (CRM, V1 mobile) : Lighthouse + Playwright mobile + V1 MOBILE CLOS → `archive/2026-04-28-sessions.md`
- Audit CRM 2026-04-04 (méthodo 5 agents, 4 sprints correctifs) → `archive/audit-crm-2026-04-04.md`

---

## NE PAS FAIRE

- Deployer sans tests (Vitest + Playwright minimum)
- Toucher des composants partages CRM sans audit cross-app

## TOUJOURS FAIRE

- Chaque etape produit un livrable concret et mesurable
- Review humaine visible dans le terminal avant tout deploy
- Tests automatises avant mise en preview

## REGLES TECHNIQUES PROJET

### Tests mobile via Chrome DevTools Device Toolbar (manuel) - OBLIGATOIRE

Tout test responsive / mobile / viewport (iPhone SE, iPhone 14 Pro Max, Pixel 7, etc.) se fait dans Chrome via **DevTools Device Toolbar** ouvert manuellement par Pascal (Cmd+Option+I → Cmd+Shift+M → sélectionner device dans le dropdown ou saisir width × height en mode "Responsive"). Claude guide la validation pas-à-pas (URL à charger, élément à inspecter, valeur attendue) ; Pascal exécute dans son DevTools et rapporte ce qu'il voit.

**Interdit** :
1. Tests Playwright avec `viewport: { width, height }` SEUL (sans `deviceScaleFactor`/`isMobile`/`hasTouch`/`userAgent`).
2. MCP `claude-in-chrome` `resize_window` + `javascript_tool` comme **substitut** à DevTools Device Toolbar (le MCP redimensionne la fenêtre Chrome desktop, pas le viewport émulé : pas de DPR mobile, pas d'UA mobile, pas de touch émulé, pas de viewport meta respecté → rendu non fidèle, écart constaté Session C 2026-04-27 : `resize_window(430, 932)` donne `innerWidth = 500`).

**Autorisé pour findings OBJECTIFS uniquement** (S125 dérogation /dig [B]) : Playwright avec `devices['iPhone 14 Pro Max']` preset complet (spread `...devices[...]` qui apporte userAgent Safari iOS + viewport 430×932 + deviceScaleFactor 3 + isMobile + hasTouch). Périmètre couvert : overflow horizontal, dimensions `getBoundingClientRect`, présence DOM, requêtes réseau (font self-host, etc.), boutons ≥ 44px, structure DOM, screenshots de référence.

**Reste obligatoire DevTools manuel pour findings QUALITATIFS** : rendu de police, perception visuelle, animations, scroll inertiel, jugement design, validation visuelle de screenshots golden. Référence d'implémentation : `CRM/tests/mobile.spec.ts` + `CRM/playwright.mobile.config.ts` (S125, 17/17 audits objectifs verts).

**Pourquoi DevTools manuel** : seule l'émulation Device Toolbar applique DPR (3 pour iPhone 14 Pro Max), UA mobile, touch émulé, viewport meta-tag, scrollbar overlay mobile → rendu fidèle au rendu réel sur device. Les media queries CSS Tailwind (`md:`, `lg:`) sont déclenchées par le viewport émulé, pas par la taille de fenêtre desktop.

**Quand le MCP reste OK** : pour mesurer `clientWidth`/`scrollWidth`/`getBoundingClientRect`/textContent en complément d'une session DevTools manuelle déjà ouverte, ou pour des audits de structure DOM non liés à la fidélité visuelle mobile.

**Comment appliquer** : si une tâche demande "tester mobile X" → demander à Pascal d'ouvrir DevTools Device Toolbar sur le viewport cible, lui donner l'URL à charger et la liste des points à vérifier (élément X visible ? overflow horizontal ? bouton Y ≥ 44px ?). Tests Playwright restent légitimes pour navigation desktop, redirects, formulaires, login (pas mobile).

Origine : Session C CRM mobile V1 2026-04-27, Pascal a explicitement refusé Playwright mobile **et** ensuite explicitement refusé MCP comme substitut à DevTools Device Toolbar manuel. Migrée du global méta vers projet AppFactory le 2026-05-01 (scope projet-only, hors-périmètre méta/Marketing/Enseignement).

## Prochaine session

**Prochaine attaque** : Bloc #1 - Livraison client lundi 2026-05-11 - 4 tâches séquentielles dont #1 livrée (Dashboard coûts API). Reprendre #2 Reporting golden v9 immédiatement au prochain démarrage. ⚠️ Inscription cockpit perdue mid-session S177 (bug watcher, voir mémoire méta `feedback_cockpit_watcher_purge_cli_entries_S177.md`) — 3 tâches restantes à recréer via cockpit UI drag manuel ou via API HTTP au boot.

### 1. Livraison client lundi 2026-05-11 [SUPERVISÉ • xhigh • cascade weekend + lundi matin]

**Pourquoi** : 4 tâches séquentielles cadrées S177 2026-05-09 pour livraison client best-in-class lundi midi 2026-05-11. Pascal demande 100% fonctionnel, sans dette technique. ⚠️ Pivot cockpit /fin-session **skippé volontairement** S177 car récidive bug watcher purge entries CLI (re-purgerait au prochain sync). Faisabilité 3 tâches restantes en weekend à valider au boot lundi (audit 360 batch + refonte Aide 3 niveaux = scope ambitieux).
**Prérequis** : aucun pour #2 (page Reporting existe), #3 attend #1+#2, #4 attend #3.

- [ ] **[EXÉCUTABLE • boot]** Tâche #0 : Restaurer le tracking cockpit S177. Drag manuel dans cockpit UI (idée → transmis) pour T2/T3/T4, OU `cli_pivot_repair.py --only appfactory`, OU exécuter le script `/tmp/restore_4_tasks_s177.py` UNIQUEMENT si le bug watcher a été corrigé entre-temps (sinon re-purge garanti). État cible : T1 [20ef5f26] delivered, T2/T3/T4 transmitted block_role=actionable order 2/3/4. → voir `~/.claude/projects/-Users-pascal--claude/memory/feedback_cockpit_watcher_purge_cli_entries_S177.md`
- [x] ~~**[EXÉCUTABLE]** Tâche #1 : Dashboard coûts API CRM `/dashboard/couts` + lien sidebar « Coûts API » au-dessus de Réduire. Skills supabase + refactoring-ui + golden-standard + test-driven-development + webapp-testing. Subagents code-review:security-auditor + bug-hunter. → voir entry Livré ci-dessous~~ - Fait 2026-05-09 (S177, ~2.5h xhigh) : Migration SQL prod + cost-tracker.persist branché veille + page UI 4 KPI + chart layerchart 12 sem + sidebar. Audit Opus 0/0/0/0/5 Info dont #1 fixé zero-debt. Tests 810/810. Smoke prod OK.
- [ ] **[EXÉCUTABLE]** Tâche #2 : Page Reporting golden v9 + remontée sidebar sous Veille sectorielle. Refonte UI page existante `/reporting` cascade gabarit golden v9 (helpers TDD + composants extraits + ARIA tablist + grille 8px stricte + audit Opus). Déplacer lien sidebar de secondary vers primary, juste sous « Veille Sectorielle ». Skills refactoring-ui + golden-standard + frontend-design + ux-guide + test-driven-development + webapp-testing. Subagents security-auditor + test-coverage-reviewer. Effort high score 2/4.
- [ ] **[BLOQUÉ - tâche #2 livrée]** Tâche #3 : Audit 360 exhaustif CRM + rapport HTML interactif + corrections. Mode 360 batch 5 agents orthogonaux parallèles (S160). Rapport HTML avec checkboxes par finding (Critique/High/Medium/Low) + bouton Approuver par section + résumé exec + screenshots inline. Pascal approuve point par point dans le HTML, corrections appliquées après validation. Skills audit-uiux + refactoring-ui + ux-guide + golden-standard + taste-skill + frontend-design + webapp-testing + dig + premortem + Defense-in-Depth-Validation + Root-Cause-Tracing. Subagents bug-hunter + code-reviewer + contracts-reviewer + security-auditor + test-coverage-reviewer + historical-context-reviewer + ui-auditor. Effort xhigh score 4/4 — `/effort xhigh` obligatoire.
- [ ] **[BLOQUÉ - tâche #3 livrée]** Tâche #4 : Refonte page Aide CRM from scratch (3 niveaux : (a) Prise en main débutants, (b) Détails toutes fonctionnalités, (c) Documentation technique architecte produit + admin). Cascade golden v9, navigation TOC, search, cross-links. Skills refactoring-ui + golden-standard + frontend-design + ux-guide + taste-skill + doc-coauthoring + webapp-testing. Subagents security-auditor + test-coverage-reviewer. Effort xhigh score 3/4.

Watch list S177 (post-livraison) :
- **[WATCH] Inscription cockpit S177 perdue mid-session** : 4 entries CLI ([20ef5f26], [b87df304], [2fcd2ed4], [27f0c30c]) inscrites via `entries.jsonl` direct ont été purgées entre 12:41 et 13:02 par un watcher cockpit (récidive bug `cli_pivot.merge_slug` S175/S179). Backup `entries.jsonl.bak.pre-4-tasks-20260509-124145`. Au boot prochaine session : restaurer via cockpit UI drag manuel (idée → transmis pour T2/T3/T4) OU via `cli_pivot_repair.py --only appfactory`. Tâche actionable méta : `~/.claude/CLAUDE.md` § Prochaine attaque.


<!-- BEGIN CONSOLIDATION (auto-géré par cockpit, ne pas éditer) -->

### Consolidation cockpit (maj 2026-05-06T16:50:06)

**Blocs actionnables** (ordre d'attaque) :

- **Bloc #1** - Refonte UI dashboard CRM (charte v9) (3.0h, confiance Élevé)
  - Objectif : Aligner /dashboard sur charte /prospection (ScorePill, TriageQueue sans fond bleu, ActionButton flat)
  - Ajuster UI page dashboard

<!-- END CONSOLIDATION -->

### 1. Cascade gabarit /prospection sur 6 pages CRM [MIXTE • xhigh • cascade 3-4 sessions]

**Pourquoi** : /prospection figée page modèle S164+S165. Cascade ordonnée des patterns golden v9 (ARIA tabs distinctifs underline, dense table avec sticky 2 cols + alignement td +4px, ScorePill, ConfirmModal, focus-visible globaux, empty states contextuels, aria-label tr, ImportModal contextuel premium 3 parcours, header condensé avec actions descendues dans tabs-bar, type Column.srLabel pour headers vides) sur les 6 autres pages CRM. Évite la dérive de design system.
**Prérequis** : aucun (page modèle figée).

- [x] **[EXÉCUTABLE]** Cadrage cascade gabarit : audit éclair des 6 pages cibles vs golden v9 (5-10 min/page). Identifier écarts critiques par page (a11y, sémantique, tokens, composants partagés). Proposer ordre d'attaque (priorité usage métier × écart). → voir `notes/audit-uiux-prospection-2026-05-01/verdict.md` § page modèle
- [x] **[BLOQUÉ - cadrage cascade validé]** Cascade page 1 : /dashboard livrée S175 (refonte v9 6 sections Bento, 7 nouveaux composants, audit Opus 0/0/0/0, prod Ready). → voir `notes/refonte-dashboard-2026-05-06/spec-implementation.md` + entry Livré S175 ci-dessous
- [x] **[BLOQUÉ - cadrage cascade validé]** Cascade page 2 : /pipeline livrée S176bis (refonte v9 cascade gabarit golden, 6 composants extraits + helpers TDD, 3 commits push origin/main). → voir entry Livré S176bis ci-dessous + `notes/refonte-pipeline-2026-05-08/spec-implementation.md`
- [x] **[BLOQUÉ - cadrage cascade validé]** Cascade page 3 : /contacts livrée S176bis (refonte v9, 2 composants Indicators/Tabs + helpers TDD contactsFormat + DataTable stickyLeftCols=2 + rowAriaLabel + zero-debt source unique normalizeCompanyName, 3 commits push origin/main).
- [x] **[BLOQUÉ - cadrage cascade validé]** Cascade page 4 : /entreprises livrée S176bis (refonte v9 toggle 2 vues Table/Cards + grille mère 8px stricte appliquée + skill refactoring-ui § Grid foundation enrichi cross-projets + fix DataTable overflow-hidden coins arrondis + 4 composants extraits + helpers TDD + audit Opus 0/0/0/0/3 Info, 2 commits push origin/main).
- [x] **[BLOQUÉ - cadrage cascade validé]** Cascade pages 5-6 : /signaux et /veille livrés S176ter (2026-05-09). /signaux = refonte v9 cards uniquement, helpers TDD signauxFormat (37 tests) + 3 composants Indicators/Tabs/Cards + 6 onglets ARIA tablist sticky + 4 KPI flat (variant warning si nouveaux > 0, success sur convertis) + FAB mobile + ConfirmModal delete et batchDelete + audit Opus 0/0/0/1 Low fixé in-session (SignalBatchDeleteSchema validation UUID + cap 500). /veille = cascade légère a11y page liste (cards archives wrappées dans `<a>` + aria-label descriptifs + h3 hierarchy + focus-visible v9, audit Opus 0/0/0/0/2 Info). Cascade gabarit 6/6 pages COMPLÈTE.
### 2. Audit zéro hallucination contenu veille W19 [SUPERVISÉ • medium • ~30-45 min]

**Pourquoi** : valider que l'édition générée via GHA (W19 vendredi 08/05 par le cron auto, OU rattrapage manuel via workflow_dispatch) respecte les critères veille standard ET que le LLM n'invente pas de contenu (chiffres, citations, dates, noms d'entreprises). Le pipeline tourne maintenant 5-10 min sans cap durée, plus de surface pour générer du contenu sourcé. Détecter toute hallucination avant utilisation côté CRM.
**Prérequis** : édition W19 publiée en DB (cron auto ou workflow_dispatch). W18 déjà publiée S167 = baseline anti-doublons.

- [x] **[LIVRÉ S176ter 2026-05-09]** Audit zéro hallucination contenu veille W19. Pour chaque item du rapport : (1) ouvrir l'URL source réelle, (2) vérifier que le titre/résumé correspond au contenu réel de la page (pas d'invention de chiffres, dates, citations, noms d'entreprises, pas d'extension d'énumération, pas de traduction technique imprécise — règles cross-check durcies S170), (3) vérifier `published_at` LLM vs date réelle (tolérance 30j fenêtre étendue), (4) flagger toute hallucination détectée. **Verdict** : 3/3 items propres, 0 hallucination grave, 1 paraphrase mineure ~100 vs 107 baux Carl-Vogt + 1 paraphrase technique façonnage vs seaming acceptables. 0 doublon URL W18/W19. Pipeline V2 S168 tient sur cron auto autonome avec taxonomie DB. → voir `memory/audit_veille_2026-05-09_W19_post_pipeline_v2.md`
### 3. Suppression handler legacy /api/cron/intelligence [SUPERVISÉ • low • ~15 min]

**Pourquoi** : info accepté audit S167 Bloc #2. Le handler SvelteKit `/api/cron/intelligence` reste vivant après désactivation du cron Vercel (rétrocompat / debug). Surface d'attaque mineure : un attaquant qui leak `CRON_SECRET` peut curl Vercel + timeout 504 (cost burn). À retirer après 2 runs GHA verts en prod (cron auto vendredi 08/05 + rattrapage W18 déjà OK = 1/2).
**Prérequis** : ≥ 2 runs GHA verts en prod (S167 = 1, attendre cron auto vendredi 08/05 = 2).

- [x] **[LIVRÉ S176ter 2026-05-09]** Supprimer `CRM/src/routes/api/cron/intelligence/+server.ts` + `CRM/src/routes/api/intelligence/trigger/+server.ts`. Cross-ref grep + retrait bypass auth `/api/intelligence/trigger` dans hooks.server.ts + docstring cost-tracker.ts updaté (cite désormais workflow GHA cron-veille comme consumer canonique). Audit Opus security-auditor : 0 C/H/M/L, 1 Info no-debt cosmétique. OWASP 11/11 verts. CRON_SECRET conservé (utilisé par 6 endpoints actifs restants). → voir `memory/audit_secu_2026-05-09_legacy_cron_removal.md`

### Watch list S171 (post-livraison)

- **[WATCH] Compteur quota search.ch côté DB (post-S171)** (vu 2026-05-06) : search.ch ne fournit pas d'API publique pour lire le compteur quota mensuel (uniquement tableau de bord compte search.ch). Pour visibilité ops « il reste N requêtes ce mois » dans CRM → table `api_quota_log` ou colonne incrémentale `searchch_calls_YYYYMM`. Pas critique tant que Pascal contrôle l'usage, à graver tâche si l'usage atteint 50%+ du quota sur un mois.
- **[WATCH] `prospect_leads.description` consommée par LLM future (audit S171 Info #1)** (vu 2026-05-06) : description = `occupation + categories.join(' / ')` (categories user-influenced via `was=` query search.ch). Aucun pipeline LLM ne consume cette colonne aujourd'hui. Si pipeline future (scoring auto, qualification, génération mail) → appliquer cross-check verbatim pattern S168. → voir `memory/audit_secu_2026-05-06_searchch_endpoint.md`.
- **[WATCH] `entry.sourceUrl` rendu href client (audit S171 Info #2)** (vu 2026-05-06) : la nouvelle colonne `prospect_leads.source_url` peut désormais pointer vers une URL search.ch arbitraire. UI rendant `<a href={sourceUrl}>` doit confirmer filtrage `https?://` côté client (defense in depth Svelte). search.ch retourne uniquement `https://tel.search.ch/...` aujourd'hui, mais aucune garantie contractuelle. Audit visuel rapide LeadSlideOut + DataTable au prochain passage.
- **[WATCH] Race condition `addItem` veille (Low #3 audit S169)** : form action `addItem` sur `/veille/[id]` fait read-modify-write non-atomique sur `intelligence_reports.items` JSONB via service client. Si 2 admins ajoutent simultanément depuis 2 onglets, le second écrase le premier (perte silencieuse, pas exploitable). Mitigation suggérée : optimistic locking via colonne `version` si volume augmente. → voir `memory/audit_secu_2026-05-05_veille_themes_db.md` Low #3.
- **[WATCH] Injection prompt LLM via description thème (Info #1 audit S169)** : la description d'un thème admin est concaténée dans `SYSTEM_PROMPT` via `buildThemesPromptSection`. Validation Zod limite à 500 chars mais aucun escape. Modèle de menace = admin authentifié déjà accès complet au CRM (faible risque). Cross-check verbatim Sonnet S168 protège la sortie LLM. Mitigation suggérée si surface s'élargit : escape `\n` + délimiter `<themes_taxonomy>` XML-like. → voir `memory/audit_secu_2026-05-05_veille_themes_db.md` Info #1.
- **[WATCH] Cron auto GHA vendredi 08/05 06h UTC W19 = premier run autonome avec taxonomie DB** : observer le run, vérifier que `loadThemeBundle` retourne bien `source: 'db'` et 10 thèmes (pas fallback hardcoded). Logs `themes_loaded` dans phase log run-generation. Si fallback déclenche → DB inaccessible (RLS, network) à investiguer.

### Livré cette session (1 récent + archives)

→ Sessions antérieures : archive/2026-05-08-sessions.md (S171-S173 + S175 dashboard v9 + S176bis x3 cascade /pipeline /contacts /entreprises) · archive/2026-05-04-sessions.md (antérieures)

- [x] ~~S177 - Dashboard coûts API CRM `/dashboard/couts` + lien sidebar « Coûts API » (tâche 1/4 livraison client lundi 2026-05-11)~~ - Fait 2026-05-09 (xhigh, ~2.5h cumulé). 3 commits push origin/main appfactory-cli sur branche `feat/dashboard-couts` mergée ff main + cleanup branche local + remote : `2c4c201` Phase A+B (migration SQL `20260509_001_cost_audit_runs.sql` 17 cols + 4 indexes + RLS authenticated SELECT, appliquée prod via Session pooler eu-west-1 IPv4 car Direct connection IPv6 timeout ; extension `cost-tracker.ts` avec méthode `persist(supabase, meta: PersistMeta)` UPSERT idempotent sur run_id, types `PersistMeta`/`PersistResult` exportés, sanitizeForLog défense-en-profondeur sur errorMessage avant upsert ; branchement `run-generation.ts` avec helpers `buildVeilleRunId(weekLabel, startedAt)` + `persistRunCosts(supabase, weekLabel, meta)` best-effort try/catch ; appels persist() aux 5 retours pipeline : 4 erreurs (exception globale, !gen.success, cross-check fatal, insert DB échoué) + 1 succès avec status=`partial` si volume < LOW_VOLUME_THRESHOLD sinon `success` ; database.types.ts régénéré incluant cost_audit_runs ; 6 nouveaux tests Vitest cost-tracker.test.ts) ; `7ad38b0` Phase C+D (helpers TDD `coutsFormat.ts` 218L + 27 tests : formatEur/Usd/Tokens/Duration/DateTime/Percent locale fr-CH SSR-safe, weekKey ISO 8601 standard, aggregateByWeek 12 sem buckets ordonnés croissants inclut semaines vides, computeKpis 30j+12sem+avg+trend7d ratio (current-previous)/previous, filterRunsByFeature/featureLabel/statusLabel ; composant `CoutsIndicators.svelte` 4 KPI flat golden v9 séparateurs verticaux 32px hairline + variant warning/success sur tendance via `derived` Svelte 5 ; composant `CoutsChart.svelte` bar chart layerchart 1.0.13 d3-scale band Axis grid + Bars radius 4 + légende tabular hover + empty state contextualisé ; +page.server.ts auth gate `safeGetSession` + SELECT `cost_audit_runs` 84j limit 500 + agrégations pré-calc serveur ; +page.svelte orchestrateur 4 KPI + chart 12 sem ; icon-map.ts ajout payments/receipt_long/trending_down/trending_flat Lucide ; config.ts item « Coûts API » en navigation.secondary après Aide juste au-dessus du bouton Réduire) ; `2350f48` simplification post-audit factuel (audit factuel : seule la veille hebdo consomme la clé Claude API CRM = 4 fichiers `intelligence/`, autres 5 crons Vercel = traitements DB déterministes ; tabs « Tous/Veille/Signaux/Autre » et colonne Feature de la table inutiles = bruit ; UI simplifiée H1 + 4 KPI cards + chart 12 sem uniquement ; serveur SELECT 5 cols au lieu de 17 ; table cost_audit_runs reste extensible CHECK constraint feature inchangé pour V2 si signaux/autre consomme Claude un jour) + `29452f7` zero-debt audit Info #1 sanitize errorMessage défense-en-profondeur dans persist(). **Workflow branche** : `git checkout -b feat/dashboard-couts` → 4 commits → `git push -u origin feat/...` → preview Vercel Ready 48s URL stable `filmpro-6o7lw0pfa-...vercel.app/dashboard/couts` → Pascal validation visuelle preview après simplification (recharge nouvelle URL post-push) → `git checkout main && git merge --ff-only` + `git push origin main` → prod Ready 54s + smoke prod `https://filmpro-crm.vercel.app/dashboard/couts` HTTP 303 → /login HTTP 200 + cleanup branche local + remote = clean. **Migration prod appliquée** via Session pooler eu-west-1 IPv4 port 5432 après échec Direct connection IPv6 (route blocked) + reset Postgres password Supabase (sécurisé `.env.local` gitignored sous clé `DATABASE_URL_ADMIN`, ZÉRO secret committé Git, ZÉRO surface Vercel envs car app utilise JWT keys SUPABASE_SERVICE_ROLE_KEY/ANON_KEY uniquement). **Tests** : Vitest **810/810 verts** (+27 vs baseline /signaux 777 + 6 cost-tracker.persist = 33 nouveaux tests). svelte-check 4/35 baseline strict maintenue (zéro nouvelle erreur). Build prod 15.14s OK. **Audit Opus security-auditor** sur diff 8 fichiers : 0 C / 0 H / 0 M / 0 L / 5 Info dont #1 fixé in-session zero-debt (sanitize errorMessage défense-en-profondeur, ceinture+bretelles côté `persist()` même si appelants amont `markError` déjà sanitize). OWASP **11/11 verts**. Surface réelle d'attaque : aucun `{@html}`, aucun input user persisté UI (table read-only, route GET-only, pas de form action), TypeScript literal union `FeatureFilter` pré-simplification empêchait injection client-side, RLS policy `auth.uid() IS NOT NULL` aligné pattern projet veille_themes/intelligence_reports, sanitizeForLog réutilisé pattern S167. Artefact `~/.claude/projects/-Users-pascal-Claude-Projets-AppFactory/memory/audit_secu_2026-05-09_dashboard_couts.md`. **Smoke prod** `https://filmpro-crm.vercel.app/dashboard/couts` HTTP 303 → /login HTTP 200 (auth gate fonctionne, RLS active, accessible aux users @filmpro.ch authentifiés via OTP). **Watchlist post-livraison** : (a) Volume runs > 500/84j si signaux+autres futures features consomment Claude → ajouter pagination ou élargir limit ; (b) RLS `auth.uid IS NOT NULL` à durcir si futur rôle restreint admin-only dashboard coûts ; (c) `error_message` lisible par tout user @filmpro.ch via SELECT RLS — audit modèle de menace si rôles introduits commercial vs ops vs admin ; (d) défense-en-profondeur Info #1 sanitize redondant côté `persist()` permet d'absorber un futur appelant qui oublierait sanitize amont ; (e) **Bug cockpit watcher purge entries CLI mid-session** récidive S177 bug `cli_pivot.merge_slug` S175/S179 — 4 entries CLI ajoutées en début de session ont été purgées par watcher (cli_pivot ou sync auto) qui reconstruit depuis CLAUDE.md, remplacées par entry inconnue [4e23ea28] dont titre vient bloc « Livré » CLAUDE.md S176ter (création à 13:02:51 entre mes inscriptions 12:41 et tentative deliver 13:44). Backup `entries.jsonl.bak.pre-4-tasks-20260509-124145` disponible. Tâche actionable corrective dans `~/.claude/CLAUDE.md` § Prochaine attaque, mémoire dédiée `~/.claude/projects/-Users-pascal--claude/memory/feedback_cockpit_watcher_purge_cli_entries_S177.md`. **Migration restructure CLAUDE.md** : entry inscrite dans `CRM/CLAUDE.md` § Prochaine session avec Bloc #1 « Livraison client lundi 2026-05-11 » (T1 cochée + T2/T3/T4 ouvertes + WATCH inscription cockpit perdue).

- [x] ~~S176ter (suite) - Tâche #2 audit zéro hallucination W19 + Tâche #3 suppression handlers legacy `/api/cron/intelligence` + `/api/intelligence/trigger` = backlog complet S176 vidé~~ - Fait 2026-05-09 (xhigh, ~1h cumulé). **Tâche #2 audit hallu W19** : cron auto GHA vendredi 08/05 SUCCESS 7m59s = 1er run autonome avec taxonomie DB livrée S169. Fetch direct DB Supabase via service role + 3 WebFetch URLs sources (RTS via curl + UA Chrome car 403 anti-bot WebFetch, glassonweb + GlobeNewswire via WebFetch direct). Verdict : 3/3 items propres (12'000 bâtiments + 0,34% vacance + Aumeunier + Berney + Rouvraie 160 résiliations 2018 + Monribeau 16 familles + Carl-Vogt = factuels RTS ; EcoArmor Vitro Solarban + Adam Polcyn = factuels glassonweb ; USD 6.42→10.42 milliards CAGR 10.2% = factuels MarketsandMarkets). 0 hallu grave. 1 paraphrase mineure ~100 vs 107 baux Carl-Vogt (signalée par tilde). 1 paraphrase technique façonnage vs seaming acceptable. 0 doublon URL W18/W19 (anti-doublons OK). Mix géo 1 SR + 2 monde (33% SR vs cible 67%, acceptable car semaine sans grosse actu locale). Volume 3 items low_volume structurel niche FilmPro (W18 aussi 3). Pipeline V2 anti-hallu S168 tient sur cron auto autonome. Artefact `~/.claude/projects/-Users-pascal-Claude-Projets-AppFactory/memory/audit_veille_2026-05-09_W19_post_pipeline_v2.md`. **Tâche #3 suppression handlers** : 1 commit push origin/main appfactory-cli sur branche `chore/remove-intelligence-legacy-handlers` mergée ff main + cleanup local + remote : `c213be8` suppression `CRM/src/routes/api/cron/intelligence/+server.ts` (-67L handler cron Vercel maxDuration:300) + `CRM/src/routes/api/intelligence/trigger/+server.ts` (-46L trigger admin) + `hooks.server.ts` retrait bypass auth `/api/intelligence/trigger` (-1 ligne) + `cost-tracker.ts` docstring updaté cite désormais workflow GHA cron-veille comme consumer canonique au lieu du handler Vercel. Audit Opus security-auditor : 0 C/H/M/L, 1 Info no-debt cosmétique, OWASP 11/11 verts. **Surface d'attaque RÉDUITE** : un attaquant ayant leaké CRON_SECRET ne peut plus déclencher timeout 504 cost-burn sur Vercel via ces 2 routes spécifiques. Aucun import orphelin (`runWeeklyGeneration` + `buildVeilleDepsFromEnvObject` restent consommés par scripts/run-veille.ts + 25 tests), aucun bypass auth dangling, CRON_SECRET conservé (utilisé par 6 endpoints actifs : signaux, alertes, nettoyage-crm, intelligence-archive, lead-rescore, recheck-historical). Tests Vitest 777/777 verts. Build prod OK. Artefact `~/.claude/projects/-Users-pascal-Claude-Projets-AppFactory/memory/audit_secu_2026-05-09_legacy_cron_removal.md`. **Backlog `[BLOQUÉ - cron auto GHA vendredi 08/05]` complètement vidé**. Cascade golden v9 6/6 + audit hallu W19 + suppression legacy = 3 livrables S176ter.

- [x] ~~S176ter - Cascade gabarit golden v9 pages 5-6 : /signaux refonte cards + /veille a11y page liste = CASCADE 6/6 COMPLÈTE~~ - Fait 2026-05-09 (xhigh, ~3h cumulé). 2 commits push origin/main appfactory-cli sur 2 branches mergées ff main + cleanup branches local + remote : `080dc34` Phase 1+2+3 helpers `src/lib/utils/signauxFormat.ts` (254L) + 37 tests Vitest TDD (signauxIndicators total/nouveaux/aConvertir/convertis, filterSignauxByTab/countsByTab 6 tabs tous/nouveau/en_analyse/interesse/converti/ecarte, emptyMessageForTab, formatTypeLabel/typeIcon/formatDate/formatRelative, scoreLabel/scoreStyle chaud/tiede/froid/non_qualifie, statutLabel/statutVariant, signalAriaLabel) + 3 composants extraits dans `src/lib/components/signaux/` (SignauxIndicators 161L 4 KPI flat avec variant warning si nouveaux > 0 + variant success sur convertis + séparateurs verticaux 32px hairline, SignauxTabs 142L ARIA tablist 6 onglets sticky + snippet actions séparateur 24px + scroll horizontal mobile, SignauxCards 268L grille auto-fill minmax(320px, 1fr) + select mode + score-pill chaud/tiède/froid + spring physics cubic-bezier(0.16,1,0.3,1) + diffusion shadow + prefers-reduced-motion respecté + line-clamp 2 description) + `+page.svelte` refondu 818 → 600L orchestrateur (header condensé bouton Ajouter + Sélectionner, 6 tabs ARIA tablist sticky remplacent 5 pills filtre statut ad-hoc, filtres type+canton dans `actions` snippet + bouton Effacer, ConfirmModal pour delete et batchDelete au lieu d'inline button toggle deleteConfirm, FAB mobile bottom-right 56×56 primary shadow rgba 45% display:grid < 768px + page-actions display:none < 768px + media queries 1024/768) + Phase 4 zero-debt audit sécu Low #1 fixé (SignalBatchDeleteSchema validation UUID + cap 500 via z.transform CSV → string[] + z.pipe + z.array(uuid).max(500), retrait .split() doublon côté +page.server.ts) ; `ea467b6` cascade légère a11y /veille page liste éditions (cards archives wrappées dans `<a href={/veille/${ed.id}}>` cliquable plein avec aria-label descriptif n° édition + week label + items count + date publication + état nouveau, `<div class="mag-archive-title">` promu en `<h3>` heading hierarchy h1 → h2 → h3, focus-visible:outline-2 outline-offset-2 outline-primary cohérent v9 sur cards et bouton featured, CTA footer "Lire cette édition" descendu en `<span>` anti nested anchors `<a>`, aucune modification autres routes du sub-arbre /veille/[id] /veille/themes /veille/item/[slug]). **Workflow branches** : `git checkout -b feat/signaux-v9` → 1 commit → push → preview Ready 42s URL stable `filmpro-e5or7bjn7-...vercel.app` → merge ff main + push prod Ready 43s + smoke `https://filmpro-crm.vercel.app/signaux` HTTP 303 → /login HTTP 200 + cleanup branche local + remote = clean. Idem `feat/veille-v9-a11y` → preview + merge + prod + cleanup. **Tests** : Vitest **777/777 verts** (+37 vs baseline /entreprises 740 = 37 tests signauxFormat). svelte-check 4/35 baseline strict maintenue (4 erreurs préexistantes intelligence/prospection non liées). Build prod 16.25s OK. **Audit Opus security-auditor** /signaux sur diff 8 fichiers +1603/-419 lignes : 0 C / 0 H / 0 M / 1 Low fixé in-session zero-debt (defense-in-depth UUID validation + cap volume) / 3 Info acceptés watchlist (aria-label Svelte 5 auto-escape, virtualisation grille volume > 5000 mono-tenant FilmPro 500-1000 attendu, notes_libres chips auto-escape). OWASP **11/11 verts**. Surface réelle d'attaque : aucun `{@html}`, aucun localStorage (contrairement à /entreprises), aucun fetch sortant, regex linéaire `formatTypeLabel`, 6 form actions Zod intactes (create/update/updateStatut/delete/deleteBatch/createOpportunite), auth gate `safeGetSession` + RLS Supabase mono-tenant intacts, CSRF SvelteKit native. Artefact `~/.claude/projects/-Users-pascal-Claude-Projets-AppFactory/memory/audit_secu_2026-05-09_signaux_v9.md`. **Audit Opus security-auditor** /veille sur diff 1 fichier +11/-9 lignes a11y-only : 0 C / 0 H / 0 M / 0 L / 2 Info watchlist (week_label brut sans coupure aria-label, defense-in-depth href interne si feature future remplace UUID par slug user-controlled). OWASP **11/11 verts**. Artefact `~/.claude/projects/-Users-pascal-Claude-Projets-AppFactory/memory/audit_secu_2026-05-09_veille_a11y.md`. **Smoke prod** `https://filmpro-crm.vercel.app/signaux` HTTP 303 → /login HTTP 200 + `https://filmpro-crm.vercel.app/veille` smoke en cours. **Critères acceptation** /signaux : 28/28 ✓ (6 tabs ARIA + 4 KPI flat + cards entité événement + FAB mobile + audit sécu 0/0/0/0 + zéro dette). /veille : 4/4 ✓ (cards cliquable plein, aria-label descriptif, h3 heading hierarchy, focus-visible v9 cohérent). **Cascade 6/6 COMPLÈTE** : /dashboard (S175) + /pipeline + /contacts + /entreprises + /signaux + /veille (S176bis-ter). Workspace pages alignées golden v9 cross-projet. **Watchlist post-livraison** : (a) Si volume signaux > 5000 → ajouter virtualisation grille (pipeline génération auto, import CSV) ; (b) cron auto GHA W19 vendredi 08/05 06h UTC = 1er run autonome avec taxonomie DB - tâche #2 audit hallucination + tâche #3 suppression handler legacy débloqués post-run vert.

- [x] ~~S174 - Migration AppFactory restructure Voie B : rename template/ → CRM/ + retrait wizard méta + Vercel PATCH rootDirectory + fix watcher cockpit + fix UI cockpit subprojects~~ - Fait 2026-05-07 (xhigh, ~2h cumulé). 2 commits push origin/main : `cd85a94` Phase 3 Voie B retrait wizard méta (-4439 lignes, 23 fichiers : `scripts/scaffold.ts` + `generate-previews.ts` + `yaml-to-config.ts` + `Wizard/cadrage/` + `Wizard/entreprise/` + `.claude/commands/cadrage.md` + `generate.md` + npm script `generate:config` + adaptations CLAUDE.md/README/start-menu.md container 3 sous-projets) ; `7cdcf29` Phase 4 Voie B `git mv template CRM` (245 fichiers renamés, refs runtime updatées : `.github/workflows/cron-veille.yml` working-directory + cache-dependency-path, `CRM/scripts/run-veille.ts` docstrings, `scripts/image-rework/*.ts` x5 PROJECT_ROOT/CRM/.env.local, README/CLAUDE.md/start-menu/SPECS_CRM_MOBILE/aide page utilisateur). **Sous-repo Formation** push commit Phase 2 `7aeaff7` sur onboarding-ia (rattrapage S173 dirty). **Méta `~/.claude`** 5 commits : `184ec73` cockpit S173 paths Formation (figeage modifs S173 jamais committées) + `e4db2e3` docs S173 + `a759b4b` cockpit Voie B paths CRM/ (slugs.py CRM cwd explicite + CLAUDE_MD_MAP CRM/CLAUDE.md + SUBPROJECT_PATHS['crm'] = CRM + tests test_slugs.py + test_hygiene_360.py) + `2317ebb` docs S174 entry Voie B + `2c5bc4b` cockpit UI fix `SUBPROJECTS_CONFIG` cockpit.html ajout consulting + appwizard (Pascal a remarqué Consulting absent du cockpit, dette S171 d'alignement front pas fait). **Vercel** : PATCH `rootDirectory: template → CRM` via API REST `https://api.vercel.com/v9/projects/prj_RB2eQOz7USRYkWqPwy3NcrnNfGoW` (PAT `vca_*` lu depuis `~/Library/Application Support/com.vercel.cli/auth.json`), preview branche `migration-voie-b-2026-05-07` Ready 50s, prod Ready 45s post-push, smoke live `https://filmpro-crm.vercel.app` HTTP 303 → /login HTTP 200, HTML CRM rendu correct. **Tags rollback** : `pre-voie-b-2026-05-07` posé sur AppFactory + `~/.claude` (1 commande `git reset --hard`). **Watcher cockpit corrigé** (régression S173 fixée) : `last_sync.watched_paths` voit désormais `AppFactory/CRM/CLAUDE.md` + `AppFactory/Formation/CLAUDE.md` (avant : pointait sur path mort `formation-ia/CLAUDE.md` et ignorait `template/CLAUDE.md`). **Mémoires Claude** : `-AppFactory-template/` renommé `-AppFactory-CRM/` (preserve 111 mémoires md S173) ; `-AppFactory-formation-ia/` legacy doublon trashé `~/.claude/trash/voie-b-legacy-20260507/` (16M) ; `-AppFactory/` 319M sources S173 conservé en backup ; `-AppFactory-Formation/` + `-AppFactory-Consulting/` actifs inchangés. **start.sh** ligne 60 : `cc 3` cd `AppFactory/CRM` (au lieu de `template`). **Wizard méta** retiré complètement, subproject cockpit `appwizard` reste référencé pour 4 entries historiques (slug cockpit-only, pattern conscient). **Cleanup branches** : `feat/veille-retrait-images` locale supprimée (ahead=0, jamais sur origin), `feat/veille-sectorielle` locale conservée (track origin pour réf historique GitHub). **Cleanup .bak** : 43 fichiers `CLAUDE.md.bak.*` racine AppFactory déplacés dans `.trash/voie-b-20260507/` (gain visuel, récupérables). **UI cockpit subprojects** : Pascal mid-session « je vois pas Consulting » → cause = `SUBPROJECTS_CONFIG` hardcoded en dur cockpit.html ligne 3577-3583 sans `consulting`. Fix : 4 sous-projets dans dropdown topbar pills (CRM, Consulting, Formation, AppWizard). Dette d'archi DRY notée (front fetch from API à considérer plus tard). **Tests verts post-migration** : Build CRM `cd CRM && npm run build` 10.74s ; svelte-check 129/33 = baseline strict zéro régression ; Vitest 666/666 ; Build Formation 1.84s ; Tests cockpit 579/579 (post-update slugs.py + tests). **Audit sécu rapide** : .env.local gitignored, 0 PAT Vercel leaké dans commits, 0 secret committé. Audit formel skippé (scope migration purement structurel zéro logique runtime). **Mémoires updatées** : `project_appfactory_restructure.md` statut Voie A+B LIVRÉE, ~/.claude/CLAUDE.md global entry S174. **Watchlist** : (a) workflow GHA cron-veille non testé en `workflow_dispatch` post-rename, cron auto vendredi 08/05 06h UTC = smoke réel ; (b) mémoire `-AppFactory/` 319M conservée backup S173 jusqu'à validation J+7 (rollback possible) ; (c) AppFactory.OLD/ 42M archive non-triée hors scope migration.

